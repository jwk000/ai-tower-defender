# 2026-06-11 开发日志

## 调整：第 2 关对称单入口蛇形路径

### 需求

第 2 关原路径不够对称，需要改成对称的单生成口蜿蜒路径；敌人前进路线必须与地图路径地块完全重合，不能出现多余路径地块。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/04-levels.md` | 更新第 2 关地图布局描述为右中出生、左中水晶、围绕中心 180 度旋转对称的蛇形路径 |
| `src/config/levels/level-02.yaml` | 重绘第 2 关 tiles、spawn 和 pathGraph，保证路径格与敌人路线一一对应 |
| `src/level/graph/__tests__/migration.test.ts` | 新增 level-02 路径地块与 pathGraph 完全重合的回归测试 |

### 验证结果

- `npm test -- src/level/graph/__tests__/migration.test.ts` 通过，19 个测试。
- `npm run typecheck` 通过。


## 功能：调试面板美术资源总开关

### 需求

调试面板新增“美术资源”开关。开启时优先使用已替换的图片美术；关闭时所有图片资源视为不可用，画面回退到原有程序化绘制效果，便于对比和定位美术资源问题。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/09-ai-art-redesign.md` | 补充美术资源调试总开关的运行时规则 |
| `src/debug/DebugManager.ts` | 新增美术资源开关状态、调试按钮和全局资源开关联动 |
| `src/debug/DebugPanel.ts` | 增加 `toggle_art_resources` 调试动作 ID |
| `src/utils/artResourceSwitch.ts` | 新增运行时图片资源总开关 |
| `src/utils/imageCache.ts` | 通用图片加载入口在开关关闭时返回不可用，触发已有 fallback |
| `src/systems/RenderSystem.ts` | 地格贴图和场景单位精灵在开关关闭时回退程序化绘制 |
| `src/systems/DecorationSystem.ts` | 背景图在开关关闭时回退天空渐变 |
| `src/utils/artResourceSwitch.test.ts` | 覆盖资源开关默认状态和切换行为 |
| `src/debug/DebugManager.test.ts` | 覆盖调试面板动作与全局资源开关联动 |

### 验证结果

- `npm run typecheck` 通过。
- `npm test -- src/debug/DebugManager.test.ts src/utils/artResourceSwitch.test.ts src/utils/artAssets.test.ts` 通过，13 个测试。
- `npm test` 通过，76 个测试文件、1302 个测试。
- `npm run build` 通过；Vite 保留现有 chunk size warning。


## 修复：AI 美术资源运行时未加载

### 问题现象

运行游戏时看不到已生成的 AI 美术资源替换效果，画面仍显示旧的程序化几何外观。

### 根因

Vite 配置的部署路径为 `base: '/ai-tower-defender/'`，但新接入的大部分图片资源使用了根路径 `/art/...`。在 `http://127.0.0.1:3000/ai-tower-defender/` 下运行时，浏览器会请求错误的 `http://127.0.0.1:3000/art/...`，资源返回 404，渲染系统因此回退到旧几何兜底。

### 改动文件

| 文件 | 改动 |
|------|------|
| `src/utils/artAssets.ts` | 新增 `assetUrl()`，统一把 `public/art` 资源路径解析到当前 Vite `BASE_URL` 下 |
| `src/utils/imageCache.ts` | 图片懒加载缓存改为使用 `assetUrl()` 后的真实 URL |
| `src/systems/RenderSystem.ts` | 地格纹理缓存改为使用 `assetUrl()`，修复路径地格图片 404 |
| `src/systems/DecorationSystem.ts` | 背景图加载改为复用 `assetUrl()`，统一资源路径策略 |
| `src/utils/artAssets.test.ts` | 新增资源路径解析测试，防止部署 base 回归 |

### 验证结果

- `curl http://127.0.0.1:3000/art/cards/card_arrow_tower.png` 返回 404，确认旧路径错误。
- `curl http://127.0.0.1:3000/ai-tower-defender/art/cards/card_arrow_tower.png` 返回 200，确认修复后的路径可访问。
- `npm run typecheck` 通过。
- `npm test` 通过，75 个测试文件、1298 个测试全部通过。

---

## 修复：UI 图片假透明与拉伸变形

### 问题现象

AI 生成的 UI 图片标称透明背景，但实际文件是 RGB 图片，透明区域被生成为灰白棋盘格像素。运行时直接绘制后，按钮、面板、卡框周围出现棋盘格背景。同时，部分 UI 图片源尺寸与运行时目标尺寸不一致，直接拉伸会导致按钮、HUD 和面板外观变形。

### 根因

1. `public/art/ui/*.png` 没有 alpha 通道，假透明棋盘格已经写入图片像素。
2. `UISystem` 对按钮、HUD、面板使用整图拉伸绘制，没有区分固定比例图片和可伸缩 UI 皮肤。
3. 横向按钮皮肤被套用到所有按钮，包括速度、暂停、图鉴等近方形小按钮。

### 改动文件

| 文件 | 改动 |
|------|------|
| `public/art/ui/*.png` | 将边缘连通的灰白棋盘格背景转为真实透明 alpha，并裁掉无效透明边界 |
| `src/utils/imageCache.ts` | 新增 `drawLoadedImage9Slice()`，支持九宫格绘制可伸缩 UI 图片 |
| `src/systems/UISystem.ts` | 面板、HUD、横向按钮改用 9-slice 绘制；小方形按钮不再套用横向按钮皮肤 |

### 验证结果

- `public/art/ui/*.png` 均为 RGBA，且 alpha 通道包含透明像素。
- `npm run typecheck` 通过。
- `npm test` 通过，75 个测试文件、1298 个测试全部通过。

---

## 修复：按钮九宫格、图鉴图片与敌人图鉴素材

### 问题现象

按钮使用 UI 皮肤后看起来像缩小贴图，没有按九宫格正确拉伸。手牌区底板边缘和空槽存在感过强，遮挡战斗画面的轻量感。卡牌图鉴仍显示旧矢量图标，没有使用已生成的卡牌插画；敌人图鉴没有独立图片资源，只能显示程序化图标。

### 根因

1. `ui_button_green.png` 去底裁边后的实际尺寸变化，原九宫格切片上下边距过大，在运行时 28px 高按钮上被压缩。
2. 手牌区同时叠加 UI 面板图、实心底板、描边和空槽描边，视觉权重过高。
3. `CardEncyclopediaUI` 和 `EnemyCodexUI` 是独立 Canvas 渲染路径，未复用战斗 HUD 中的图片加载逻辑。
4. 生成资产清单未包含敌人图鉴图片。

### 改动文件

| 文件 | 改动 |
|------|------|
| `src/systems/UISystem.ts` | 调整按钮九宫格切片，横向按钮优先绘制图片皮肤；降低手牌底板和空槽透明度、描边强度 |
| `src/ui/CardEncyclopediaUI.ts` | 卡牌图鉴优先加载 `public/art/cards/card_<id>.png`，失败时回退矢量图标 |
| `src/ui/EnemyCodexUI.ts` | 敌人图鉴优先加载 `public/art/enemies/enemy_<id>.png`，失败时回退矢量图标 |
| `src/utils/artAssets.ts` | 新增 `enemyArtPath()` |
| `scripts/generate-ai-art-assets.mjs` | 将 24 个敌人图鉴素材加入统一模型生成清单 |
| `public/art/enemies/*.png` | 新增 24 张敌人图鉴素材，统一使用 `azure/gpt-image-2` 生成并本地去底裁边 |
| `art-generation-progress.json` | 记录 24 个敌人图鉴素材生成进度，全部 `done` |
| `design/09-ai-art-redesign.md` | 补充敌人图鉴素材路径和尺寸规范 |

### 验证结果

- 24 张 `public/art/enemies/*.png` 均为 RGBA，且 alpha 通道包含透明像素。
- `art-generation-progress.json` 中敌人素材全部 `done`，`modelLock` 保持 `azure/gpt-image-2`。

---

## 修复：场景单位美术资源接入

### 问题现象

战斗场景中的塔、士兵、敌人、机关等单位仍然全部使用代码生成的几何图形，没有替换成图片资源。

### 处理策略

1. 保留原有 `visualParts` / `drawUnitComposite` / 陷阱 switch 绘制逻辑，作为资源缺失或加载失败时的兜底。
2. 运行时优先加载 `public/art/units/unit_<unit_id>_idle_<frame>.png`，通过呼吸相位在 0/1 两帧之间切换。
3. 继续复用现有代码动画和状态叠加，包括攻击缩放、移动朝向、受击闪白、冰冻、眩晕、中毒、精英描边和 Boss 阶段效果。
4. 场景单位 AI 直接生成在当前网关两次读超时，因此本轮先用已经由 `azure/gpt-image-2` 生成的卡牌图和敌人图派生 94 张透明场景精灵帧；生成脚本和进度清单仍保留直接生成任务，后续接口稳定后可继续覆盖。

### 改动文件

| 文件 | 改动 |
|------|------|
| `src/systems/RenderSystem.ts` | 新增场景单位图片缓存和绘制路径；塔/士兵/敌人/机关优先使用图片，失败回退程序化绘制 |
| `src/render/Renderer.ts` | 支持图片渲染时按 `scaleX` 水平翻转，保留单位朝向 |
| `src/systems/WaveSystem.ts` | 敌人生成时写入类型索引，保证场景敌人能匹配到正确素材 |
| `src/utils/artAssets.ts` | 新增 `unitArtPath()` |
| `scripts/generate-ai-art-assets.mjs` | 补充 47 个场景单位、每个 2 帧的直接生成清单 |
| `public/art/units/*.png` | 新增 94 张场景单位 idle 透明帧 |
| `art-generation-progress.json` | 记录 94 张场景单位帧为 `done`，并标注当前由同模型既有素材本地派生 |
| `design/09-ai-art-redesign.md` | 补充场景单位运行时接入和兜底规则 |

### 验证结果

- `npm run typecheck` 通过。
- `npm test` 通过，75 个测试文件、1300 个测试。
- 94 张 `public/art/units/*.png` 均存在，资源进度清单单位项全部 `done`。

---

## 修复：法术释放特效假透明背景

### 问题现象

火球、箭雨、暴风雪、炸弹等法术卡释放后，投射物拖尾路径和命中特效图片带有灰底或棋盘格背景，叠加到场景上会出现明显方形底色。

### 根因

`public/art/fx/` 下法术释放链路实际引用的 FX 图片由 AI 生成后仍是 RGB PNG，提示词里的透明背景没有变成真实 alpha 通道。运行时 `SpellProjectileSystem` 正常按图片绘制，但资源本身没有透明区域。

### 改动文件

| 文件 | 改动 |
|------|------|
| `public/art/fx/fx_fire_explosion_charge_0.png` | 本地去底，输出 RGBA 真透明 PNG |
| `public/art/fx/fx_fire_explosion_impact_1.png` | 本地去底，输出 RGBA 真透明 PNG |
| `public/art/fx/fx_arrow_rain_impact_1.png` | 本地去底，输出 RGBA 真透明 PNG |
| `public/art/fx/fx_ice_burst_impact_1.png` | 本地去底，输出 RGBA 真透明 PNG |
| `public/art/fx/fx_bomb_explosion_impact_1.png` | 本地去底，输出 RGBA 真透明 PNG |
| `src/utils/artAssets.test.ts` | 补充法术投射物和命中特效路径契约测试 |
| `design/09-ai-art-redesign.md` | 补充法术投射物、拖尾、释放效果必须为真实 alpha PNG 的验收规则 |

### 验证结果

- 5 张法术释放链路 FX 图片均为 RGBA，且 alpha 通道包含透明像素。
- `npm run typecheck` 通过。
- `npm test -- src/utils/artAssets.test.ts` 通过。

---

## 调整：路径地格改为无方向均匀地块

### 需求变化

路径地格不再设计直线、拐角、三通、十字等方向连接件。普通路径统一使用颜色和纹理均匀、无明确方向的地格；多个路径地格相邻时只通过色块聚合看起来像连续路径。

### 改动文件

| 文件 | 改动 |
|------|------|
| `src/utils/pathTileTexture.ts` | 移除路径方向邻接判断，`TileType.Path` 固定返回 `tile_<theme>_path.png` |
| `src/utils/pathTileTexture.test.ts` | 改为验证直线、拐角、三通、十字布局都使用同一张无方向路径贴图 |
| `scripts/generate-ai-art-assets.mjs` | 生成清单不再追加直线、拐角、三通、十字路径件，仅保留基础路径和端点图 |
| `design/09-ai-art-redesign.md` | 更新路径地格美术规则、提示词、运行时接入状态 |
| `design/art-path-tile-connectors.json` | 从方向连接件契约改为无方向路径地格契约 |

### 验证结果

- `npm test -- src/utils/pathTileTexture.test.ts` 通过，6 个测试。
- `npm run typecheck` 通过。
- `design/art-path-tile-connectors.json` 可正常解析。

---

## 修复：第 5 关深渊地格过暗

### 问题现象

第 5 关深渊裂隙的场景地格整体太暗，深色单位放在棋盘上时轮廓不够清楚。

### 处理策略

1. 提亮 `abyss` 主题基础地格、路径地格、障碍地格、出生口端点和水晶口端点图片，保持紫灰深渊氛围，但提高地面平均亮度。
2. 同步调亮第 5 关 `tileColors`，保证图片未加载或回退到纯色绘制时仍然可读。
3. 更新美术文档中深渊地格提示词，明确“不能太暗，必须保证单位轮廓可读”。

### 改动文件

| 文件 | 改动 |
|------|------|
| `public/art/tiles/tile_abyss_*.png` | 提亮第 5 关使用的 5 张深渊主题地格图 |
| `src/config/levels/level-05.yaml` | 调亮深渊地图 fallback 地格色 |
| `design/09-ai-art-redesign.md` | 更新深渊主题地格提示词 |
| `devlog/2026-06-11.md` | 记录本次修复 |

### 验证结果

- 5 张深渊主题地格图平均亮度提升到 0.25-0.31 区间。
- `npm test -- src/editor/state/__tests__/moonlightConfig.test.ts` 通过，2 个测试。
- `npm run typecheck` 通过。

---

## 调整：胜利界面合并为单层展示

### 需求变化

胜利界面不再同时显示旧结算层和故事面板层，改为单层覆盖层顺序展示：通关标题、彩带下落、星星评定、无底框剧情文字。

### 改动文件

| 文件 | 改动 |
|------|------|
| `src/systems/VictoryScreenSystem.ts` | 胜利态改为“恭喜你通关关卡名”标题常驻；彩带、星星、剧情文字在同一覆盖层内按阶段浮现；剧情文字去掉底框并放大；胜利态整屏点击继续 |
| `src/systems/UISystem.ts` | 关闭 `GamePhase.Victory` 下旧的“胜利!/刷新页面重新开始”覆盖层，避免双层 UI |
| `src/main.ts` | 启动胜利覆盖层时传入关卡名 |
| `design/04-levels.md` | 更新过关界面阶段说明为单层 3 阶段动效 |
| `design/05-presentation.md` | 更新胜利界面渲染架构、时序和视觉规范 |

### 验证结果

- `npm run typecheck` 通过。
- 浏览器验证第 1 关调试通关：旧胜利层消失；“恭喜你通关绿野仙踪”、彩带、星星、无底框剧情文字均正常显示。

---

## 调整：失败界面合并为单层并增加调试失败按钮

### 需求变化

失败界面也需要去掉旧覆盖层和故事面板的双层 UI，改为单层覆盖层直接显示失败剧情标题，并让剧情正文以打字机效果逐字出现。同时调试面板需要提供战斗中直接触发失败的按钮，方便测试失败界面。

### 改动文件

| 文件 | 改动 |
|------|------|
| `src/systems/VictoryScreenSystem.ts` | 失败态改为单层覆盖层；直接显示失败剧情标题；剧情正文无底框并以打字机效果逐字显示；正文完成后整屏点击返回 |
| `src/systems/UISystem.ts` | 关闭 `GamePhase.Defeat` 下旧的“失败!/刷新页面重新开始”覆盖层 |
| `src/debug/DebugPanel.ts` | 扩展调试动作类型，加入 `skip_to_defeat` |
| `src/debug/DebugManager.ts` | 调试面板新增“直接失败（测试用）”按钮 |
| `src/main.ts` | 增加失败处理守卫，避免每帧重置失败界面；接入调试直接失败入口；缺省失败故事使用兜底文案 |
| `design/05-presentation.md` | 更新失败界面单层覆盖层规范 |

### 验证结果

- `npm run typecheck` 通过。
- 浏览器验证第 1 关战斗中点击“直接失败（测试用）”：失败界面显示失败剧情标题、无底框打字机剧情文字和“点击返回”；点击后返回关卡选择界面。

---

## 调整：胜利剧情正文增加打字机效果

### 需求变化

胜利界面的剧情正文也需要和失败界面一致，直接显示在界面上但以打字机效果逐字出现，正文打完后再允许点击继续。

### 改动文件

| 文件 | 改动 |
|------|------|
| `src/systems/VictoryScreenSystem.ts` | 胜利 Phase3 剧情正文改为按 `phaseElapsed` 逐字显示；正文完成后再显示“点击继续” |
| `design/05-presentation.md` | 更新胜利 Phase3 时序和视觉规范，明确剧情段落使用打字机效果 |

### 验证结果

- `npm run typecheck` 通过。
- `npm run build` 通过。
- 浏览器验证第 1 关调试通关：胜利剧情正文逐字显示，正文完成后才出现“点击继续”。

---

## 修复：重生成所有关卡路径均匀纹理

### 需求变化

所有关卡路径图片不应包含单块道路方向、路缘或端点装饰，只保留均匀地表纹理。路径形状由多个均匀纹理拼接形成。

### 改动文件

| 文件 | 改动 |
|------|------|
| `public/art/tiles/tile_*_path*.png` | 重新生成 meadow/desert/castle/wasteland/abyss 五套路径纹理，并覆盖直线、拐角、三岔、十字、端点等历史变体 |
| `devlog/2026-06-11.md` | 记录本次路径纹理修复 |

### 验证结果

- 已检查 5 个主题基础路径纹理缩略图，确认不再包含道路方向、路缘、脚印、石板线或端点装饰。
- `npm test -- src/utils/pathTileTexture.test.ts` 通过，6 个测试。
- `npm run build` 通过；仅保留 Vite chunk size 常规警告。

---

## 补充：水晶和出生口独立美术资源

### 需求变化

水晶和敌人出生口不能再依赖路径端点贴图表达。路径端点只保留均匀底图，水晶/出生口必须拥有独立主体资源，并各自搭配循环特效资源。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/05-presentation.md` | 补充路径端点美术资源契约，明确主体资源、循环 FX、层级和兜底规则 |
| `design/art-path-tile-connectors.json` | 更新端点说明，新增出生口/水晶 overlay 资源映射 |
| `public/art/objectives/*.png` | 新增水晶、低血量水晶、敌人出生口透明 PNG 主体资源 |
| `public/art/fx/fx_crystal_aura_loop_0.png` | 新增水晶循环光效资源 |
| `public/art/fx/fx_spawn_portal_loop_0.png` | 新增出生口循环光效资源 |
| `scripts/generate-objective-art-assets.mjs` | 新增可复现的本地透明 PNG 资源生成脚本 |
| `src/utils/artAssets.ts` / `src/utils/artAssets.test.ts` | 新增 objective 主体和 FX 路径 helper 与测试 |
| `src/systems/RenderSystem.ts` | 出生口叠加主体/FX 图片；水晶按血量叠加主体/FX 图片，保留程序化动态特效和加载兜底 |

### 验证结果

- 资源文件均为 512×512 RGBA PNG，角点透明。
- `npm test -- src/utils/artAssets.test.ts src/systems/RenderSystem.crystal.test.ts` 通过，7 个测试。
- `npm run build` 通过；仅保留 Vite chunk size 常规警告。
- 浏览器验证第 1 关：左侧水晶主体和光效可见，右侧敌人出生口显示黑红传送门并带环形特效。

---

## 调整：入场动画按阶段替换美术地格

### 需求变化

入场动画需要先随机下落普通场景地格；随后水晶地格替换水晶位置，出生口地格替换出生口位置；最后路径按 `pathGraph` 从出生口到水晶每 20ms 替换一个路径地格，全部铺完后动画完成。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/05-presentation.md` | 更新 Level Intro 时间线、阶段语义、路径替换算法和 20ms/格节奏 |
| `src/systems/LevelIntroSystem.ts` | 入场动画改为按阶段选择当前地格类型，并优先绘制美术资源；路径按所有 spawn 的 `pathGraph` 有向边逐格替换 |
| `src/systems/RenderSystem.ts` | 水晶渲染整体响应 `Visual.alpha`；实体 alpha 为 0 时不绘制血条，避免入场前水晶血条提前出现 |
| `src/utils/pathTileTexture.ts` / `src/utils/pathTileTexture.test.ts` | 新增按 TileType 获取贴图路径的 helper，供入场动画复用正式地格资源 |
| `devlog/2026-06-11.md` | 记录本次入场动画调整 |

### 验证结果

- `npm test -- src/utils/pathTileTexture.test.ts src/utils/artAssets.test.ts src/systems/RenderSystem.crystal.test.ts` 通过，14 个测试。
- `npm run build` 通过；仅保留 Vite chunk size 常规警告。
- 浏览器验证第 1 关：首阶段只显示普通场景地格随机下落，水晶/血条不提前显示；后续水晶、出生口和路径按阶段替换。

---

## 调整：图片背景开启时隐藏程序化天气地标

### 需求变化

开启关卡图片背景时，程序化天气地标不能再叠在背景图上：晴天隐藏太阳和云朵，夜晚隐藏月亮，红雾隐藏程序化远景山体和“眼睛”夕阳；关闭图片背景或美术资源总开关后恢复程序化表现。

### 改动文件

| 文件 | 改动 |
|------|------|
| `src/systems/DecorationSystem.ts` | 统一有效图片背景判断；图片背景开启时跳过晴天粒子云，关闭后恢复 |
| `src/systems/ScreenFXSystem.ts` | 新增 `backgroundImageActive` 渲染参数；图片背景开启时跳过太阳、阳光光束和月亮 |
| `src/main.ts` | 将调试背景图开关与美术资源总开关合并为有效背景状态传给 `ScreenFXSystem` |
| `src/systems/ScreenFXSystem.test.ts` | 增加晴天和夜晚图片背景天空地标隐藏测试 |
| `devlog/2026-06-11.md` | 记录本次天气显示调整 |

### 验证结果

- `npm test -- ScreenFXSystem` 通过，7 个测试。
- `npm run typecheck` 通过。

---

## 调整：入场动画地格替换增加破碎效果

### 需求变化

场景地格被水晶、敌人出生口或路径地格替换时，需要增加旧普通场景地格的破碎反馈，强化入场动画中战场地貌被改写的表现。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/05-presentation.md` | 补充地格替换破碎效果的触发范围、碎片结构、层级、时长和完成条件 |
| `src/systems/LevelIntroSystem.ts` | 新增普通地格破碎临时特效队列；水晶、出生口、路径替换时触发 4×4 贴图碎片、尘屑、描边和淡出动画 |
| `devlog/2026-06-11.md` | 记录本次入场动画破碎效果调整 |

### 验证结果

- `npm test -- src/utils/pathTileTexture.test.ts src/utils/artAssets.test.ts src/systems/RenderSystem.crystal.test.ts` 通过，14 个测试。
- `npm run build` 通过；仅保留 Vite chunk size 常规警告。
- 浏览器验证首关入场：普通地格先完整下落；水晶位置替换时旧地格碎片和尘屑可见；出生口替换后传送门主体正常出现。

---

## 修复：敌人图片左右朝向跟随路径方向

### 需求变化

敌人图片左右朝向必须和路径水平前进方向一致；当敌人从竖直段切换到向左或向右的新路径段时，切换帧就要同步翻转。纯竖直移动段保留既有左右朝向。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/02-gameplay.md` | 补充敌人路径移动的左右朝向规则 |
| `src/systems/MovementSystem.ts` | 敌人朝向改为按当前/下一路径段同步，覆盖移动、拐点切换和路径恢复 |
| `src/systems/__tests__/MovementSystem.pathEquivalence.test.ts` | 增加竖直到左转、竖直到右转的切换帧朝向测试 |

### 验证结果

- `npm test -- src/systems/__tests__/MovementSystem.pathEquivalence.test.ts src/systems/MovementSystem.test.ts` 通过，18 个测试。
- `npm run typecheck` 通过。
- `npm run build` 通过；仅保留 Vite chunk size 常规警告。
