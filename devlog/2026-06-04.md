# 开发日志 — 2026-06-04

## 任务：导弹塔预览标记 + 弹坑效果修复

### 需求
1. 导弹塔的导弹攻击半径应该和预览标记（红色靶心圈）的半径一样大
2. 地上的弹坑不用和格子对齐，弹坑应该和攻击范围对齐（圆形弹坑）

### 改动文件
| 文件 | 改动 |
|---|---|
| `src/core/components.ts` | `TileDamageMark` 组件新增 `craterRadius: Types.f32` 字段，支持圆形弹坑模式 |
| `src/systems/AttackSystem.ts` | TargetingMark `blastRadius` 从硬编码 `120` 改为读取 `Attack.splashRadius[eid]`（实际值 80px） |
| `src/systems/TileDamageSystem.ts` | 完全重写：从逐格裂纹弹坑改为以命中点为中心的圆形弹坑 |
| `src/systems/RenderSystem.ts` | `drawTileDamageMarks()` 新增圆形弹坑渲染路径（PixiJS 命令） |

### 改动详情

#### 1. 预览标记半径修复（AttackSystem.ts）
- **问题**：TargetingMark 的 `blastRadius` 硬编码为 `120`，但实际导弹溅射半径从塔配置读取为 `80`（gameData.ts `splashRadius: 80`）
- **修复**：改为从 `Attack.splashRadius[eid]` 读取塔的实际溅射半径，与 `spawnMissileProjectile` 保持一致
- **视觉效果**：红色靶心环半径从 120px 缩小到 80px，与爆炸伤害范围完全一致

#### 2. 圆形弹坑效果（TileDamageSystem + RenderSystem）
- **问题**：旧实现遍历所有格子，在每个被爆炸覆盖的格子内绘制独立裂纹——弹坑与格子对齐，不自然
- **修复**：改为以命中点 `(hitX, hitY)` 为中心，半径为 `blastRadius` 的圆形弹坑实体
- **渲染效果**（双通道）：
  - Canvas 2D（`TileDamageSystem.render`）：径向渐变暗色填充 + 边缘环 + 锯齿状辐射裂纹 + 碎片散射
  - PixiJS（`RenderSystem.drawTileDamageMarks`）：大圆形填充 + 环 + 旋转矩形裂纹 + 碎片小圆点
- **去重**：同一弹坑位置重复命中时刷新计时器而非创建新实体
- **Fallback**：`craterRadius === 0` 时保留旧的逐格模式（向后兼容）

### 验证结果
- ✅ TypeScript 类型检查：4 个修改文件零错误（`TrapSystem.test.ts` 的 `hpBefore` 错误为已有问题）
- ✅ vitest 全量测试：62 文件 / 1257 测试全部通过
- ⚠️ `npm run release` 因已有的 `TrapSystem.test.ts` 类型错误 + esbuild top-level await 配置问题而失败（与本次改动无关）
- ✅ LSP 诊断：4 个文件零错误

### 提交
`67ecc96` feat: 导弹塔预览标记半径与实际爆炸范围一致，弹坑改为圆形攻击范围对齐

---

## 任务：敌人攻击动画 — 脉冲波形 + 冲撞缩放 + 受击反馈

### 需求
敌人到达基地时没有任何攻击动作，完全看不出来。敌人的视觉表现都是球形，需要设计明显的攻击动画。

### 设计方案（Phase 1+2 已实现）
- **受击反馈**：基地触发 `hitFlashTimer`（白色闪烁 120ms）+ `ScreenShakeSystem.triggerShake`（微震 3px/0.25s）
- **近战冲撞动画（squash-stretch）**：敌人缩进（0-35%）→ 弹向目标方向 + 放大（35-60%）→ 回弹（60-100%），持续 0.4s
- **脉冲波形**：3 层同心圆从敌人位置向外扩散并衰减，增强冲击感

### 改动文件
| 文件 | 改动 |
|---|---|
| `src/systems/MovementSystem.ts` | 新增攻击动画生命周期：首帧到达→触发攻击动画+伤害→0.4s后销毁；移除旧 `onReachEnd` 方法；导入 `ScreenShakeSystem` |
| `src/systems/RenderSystem.ts` | `drawEntities()` 新增敌人攻击动画渲染：squash-stretch 变形 + 脉冲波同心圆；区分简单形状敌人和有 `partsId` 的复合敌人 |
| `src/systems/MovementSystem.test.ts` | 更新"到达终点的敌人被销毁"测试：改为验证攻击动画生命周期（3帧流程：到达→动画中→销毁） |

### 实现细节

#### MovementSystem — 攻击动画状态机
- **首帧到达**：`attackDur === 0` → 进入 Case 2 → 造成伤害 + 设置 `attackAnimTimer/attackAnimDuration = 0.4` + 受击闪光 + 屏幕震动
- **动画进行中**：`attackAnimTimer > 0` → 暂停移动帧衰减计时器 → `continue`
- **动画结束**：`attackAnimTimer <= 0 && attackDur > 0` → Case 1 → `destroyEntity`
- **设计要点**：`attackAnimTimer` 在首帧到达后设置（不在顶部检查中衰减），第 2 帧开始衰减

#### RenderSystem — 双轨渲染
- 简单形状敌人（`partsId === 0`）：直接渲染带动画参数的形状
- 复合部件敌人（`partsId !== 0`）：传递给 `drawSoldierComposite`
- 动画参数通过 `attackAnimPosX/Y/Size` 局部变量传递，避免修改原始的 `const posX/posY`
- 脉冲波与实体同层（z+1），白色半透明描边圆

### 验证结果
- ✅ TypeScript 类型检查：仅 `TrapSystem.test.ts:252` 已有的 `hpBefore` 可能 undefined 错误
- ✅ vitest 全量测试：62 文件 / 1261 测试全部通过
- ✅ LSP 诊断：修改文件全部零错误

### 提交
`cb13aaf` feat: 敌人到达基地时触发0.4s攻击动画 — 脉冲波形+冲撞缩放+基地受击闪烁+屏幕微震

### 待实现（Phase 2 后续）
- 远程敌人（wizard/priest）发射小型弹道
- 根据敌人类型配置不同攻击风格（近战冲撞 vs 远程弹道 vs AOE冲击）

---

## 任务：天气视觉效果增强 — 雾粒子、雨滴、乌云、星星、月亮、鸟/云天气适配

### 需求
1. 雾天增加粒子雾效飘荡在场景上
2. 雨天增加雨滴掉落效果 + 乌云粒子特效
3. 只有晴天有白云飘荡
4. 晴天场景中的鸟是白色的，体型放大一些
5. 夜晚场景中的鸟是黑色的，增加红色眼睛
6. 夜晚的天空背景增加闪烁的星星 + 闪着光晕的大月亮（右上角）

### 改动文件
| 文件 | 改动 |
|---|---|
| `src/systems/DecorationSystem.ts` | 导入修正（`type WeatherType` → `WeatherType`）；`drawClouds()` 仅在晴天绘制云朵；`drawBirds()` 根据天气动态调整鸟的颜色和大小（晴天白色1.5x、夜晚黑色+红眼） |
| `src/systems/ScreenFXSystem.ts` | 新增 5 个全屏特效方法：`drawFogParticles`（雾粒子）、`drawRainDrops`（雨滴）、`drawDarkClouds`（乌云）、`drawStars`（星空）、`drawMoon`（月亮+光晕） |

### 实现细节

#### DecorationSystem — 天气感知装饰
- **云朵**：`drawClouds()` 新增 `if (this.getWeather() !== WeatherType.Sunny) return;`，仅在晴天绘制
- **鸟**：
  - 晴天：`birdColor = '#ffffff'`（纯白），`birdSize = bird.size * 1.5`
  - 夜晚：`birdColor = '#111111'`（黑色），额外渲染红色眼睛（`#ff0000`，size=2）
  - 其他天气：保持原有主题色
  - 所有值通过局部变量覆盖，不修改 BirdState 对象

#### ScreenFXSystem — 新特效方法

| 方法 | 天气 | 参数 |
|---|---|---|
| `drawFogParticles` | Fog | 10个雾团（`#d0d8e0`），半径80-200px，α=0.03-0.08，水平漂移20-50px/s + 正弦纵向摆动 |
| `drawRainDrops` | Rain | 40条雨痕（`#a0c4e8`），1-2px宽/15-30px长，α=0.3-0.6，下落300-600px/s |
| `drawDarkClouds` | Rain | 5个椭圆乌云（`#2a3040`），α=0.15-0.3，位于Y:0-200，水平漂移15-35px/s |
| `drawStars` | Night | 40颗星星（`#ffffff`），半径1-3px，Y:0-300，`Math.sin`闪烁1.5-4Hz |
| `drawMoon` | Night | 右上角（DESIGN_W*0.85, 100），半径55px（`#fffde7`），3层光晕（80/110/150px递减α）+ 呼吸脉冲 |

- 所有粒子使用 Knuth 乘法哈希确定性种子，帧间位置一致
- `render()` 方法按序调用所有特效，暗角效果保持在最后

### 验证结果
- ✅ TypeScript 类型检查：仅 `TrapSystem.test.ts:252` 已有的 `hpBefore` 可能 undefined 错误
- ✅ vitest 全量测试：62 文件 / 1261 测试全部通过
- ✅ LSP 诊断：修改文件全部零错误

### 提交
`fe7cf41` feat: 天气视觉效果增强 — 雾粒子、雨滴、乌云、星星、月亮，鸟/云天气适配
