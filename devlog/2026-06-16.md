## 修复：单位死亡灰飞烟灭效果不出现

### 问题

单位死亡时会创建 `DeathEffect` / `DisintegrateEffect` 实体，但 `DeathEffectSystem` 在 `RenderSystem` 前运行。若创建后的首帧 `dt` 偏大，死亡特效实体可能在被渲染前就进入清理，导致灰飞烟灭效果完全看不到。

### 改动

| 文件 | 改动 |
|------|------|
| `src/core/components.ts` | 为 `DeathEffect` 增加 `renderedFrames` 字段，记录死亡特效是否至少经过一次渲染 |
| `src/systems/DeathEffectSystem.ts` | 未渲染过的死亡特效不推进计时和清理，避免首帧前消失 |
| `src/systems/RenderSystem.ts` | 渲染死亡特效实体时递增 `renderedFrames` |
| `src/systems/LifecycleSystem.ts`、`src/core/RuleHandlers.ts`、`src/main.ts` | 创建死亡特效时显式初始化 `elapsed` 和 `renderedFrames` |
| `src/systems/RenderSystem.crystal.test.ts`、`src/systems/LifecycleSystem.test.ts` | 增加首帧可见性回归测试，并更新清理测试的新语义 |

### 验证结果

- `npm test -- src/systems/RenderSystem.crystal.test.ts src/systems/LifecycleSystem.test.ts src/core/RuleHandlers.test.ts` 通过，76 个用例。
- `npm run typecheck` 通过。

---

## 修复：音效设计资源调用链路校验

### 需求

检查代码是否真正调用了设计中的音效资源。

### 检查结果

- `Sound.ts` 声明的 70 个 SFX key 与代码/YAML/关卡音频配置交叉比对后，资源文件均存在。
- 发现 YAML 规则里仍使用 `SFX_ENEMY_DIE`、`SFX_BOSS_SPAWN` 等旧设计常量，原逻辑会直接传给 `Sound.play()` 并被静默丢弃。
- 发现关卡胜利配置使用 `bgm/victory_xxx.ogg`、`sfx/victory_xxx.ogg` 路径形式，原胜利播放逻辑直接传给 `Music.play()` / `Sound.play()`，不会命中 key。

### 改动

| 文件 | 改动 |
|------|------|
| `src/utils/Sound.ts` | 新增 `normalizeSfxKey()`，兼容真实 key、`sfx/*.ogg` 路径和旧 `SFX_*` 常量别名 |
| `src/core/RuleHandlers.ts` | `play_sound` 规则播放前先归一化音效 key，无法识别时跳过 |
| `src/main.ts` | 胜利音频播放兼容 `bgm/*.ogg` / `sfx/*.ogg` 资源路径配置 |
| `src/core/RuleHandlers.test.ts` | 增加旧常量别名和路径形式音效配置的回归测试 |

### 验证结果

- 集合比对确认：`unknownYaml=[]`，`unknownDirectSfx=[]`，`unknownLevelSfx=[]`，`unknownDirectBgm=[]`，`unknownLevelBgm=[]`。
- `npm test -- src/core/RuleHandlers.test.ts` 通过，59 个用例。
- `npm run typecheck` 通过。

---

## 审计：敌方单位与 Boss 音效覆盖

### 需求

检查敌方单位音效是否齐全，重点关注 Boss 音效还需要补充哪些。

### 检查结果

- 当前 5 个已配置关卡实际会用到的敌方单位均可命中已有音效 key，未发现资源文件缺失。
- 普通敌人主要共用 `enemy_spawn`、`enemy_death`、`enemy_attack`、`mage_attack`、`poison_hit`、`stun_apply`、`soldier_heal` 等通用音效。
- 当前 Boss 均有出场和死亡音效链路：`SFX_BOSS_SPAWN -> wave_boss`，`SFX_BOSS_DIE -> victory`。
- Boss 技能已有 4 类通用音效：召唤 `boss_summon`、阶段/控制 `boss_phase2`、导弹/扭曲 `boss_missile`、吞噬/AOE `boss_devour`，巨型史莱姆分裂另有 `boss_split`。

### 缺口判断

- 普通敌人资源齐全但粒度偏粗，重装、机械、飞行、自爆、治疗/复活类敌人缺少专属事件音色。
- Boss 资源不缺文件，但表现层不够“Boss 专属”：14 个 Boss 共用同一出场和死亡音效，多个技能复用 `boss_summon` / `boss_phase2` / `boss_missile` / `boss_devour`。
- 最高优先级应补：Boss 专属出场、Boss 专属死亡、阶段转换、召唤、导弹预警/落地、吞噬、分裂、狂暴倒计时。

### 建议补充清单

| 优先级 | 建议 key | 用途 |
|--------|----------|------|
| P0 | `boss_enter_slime`、`boss_enter_beetle`、`boss_enter_lucifer`、`boss_enter_robot`、`boss_enter_abyss` | 5 个主线 Boss 出场差异化 |
| P0 | `boss_death_heavy`、`boss_death_void` | Boss 死亡区别于普通胜利短音效 |
| P0 | `boss_missile_warning`、`boss_missile_impact` | 超级机器人导弹预警和落地分离 |
| P0 | `boss_devour_cast`、`boss_devour_impact` | 深渊领主吞噬施法和命中分离 |
| P1 | `boss_summon_insect`、`boss_summon_undead`、`boss_summon_machine`、`boss_summon_void` | Boss 召唤按阵营区分 |
| P1 | `boss_phase_ice`、`boss_phase_void`、`boss_phase_enrage` | 阶段转换按主题区分 |
| P1 | `enemy_death_heavy`、`enemy_death_magic`、`enemy_death_machine`、`enemy_death_flying` | 普通敌人死亡按类型区分 |
| P2 | `enemy_spawn_flying`、`enemy_spawn_machine`、`enemy_spawn_undead` | 普通敌人出生按类型区分 |

---

## 排查：第4关敌人半路停住

### 现象

第4关战斗截图中，大批敌人在中路交战区域聚集，看起来没有继续向水晶推进。

### 排查结果

- 第4关 `pathGraph` 三条出生路径均有完整后继，`spawn_0`、`spawn_1`、`spawn_2` 都连接到 `crystal_anchor`，未发现路径断点。
- MovementSystem 在敌人 FollowPath 移动前会先检查攻击：只要敌人有 `Attack` 组件且范围内存在士兵或塔，就执行攻击并把 `Movement.currentSpeed` 置为 0，本帧不推进路径。
- 攻击动画期间 `Visual.attackAnimTimer > 0` 时同样会停止移动。截图中的弓手位于中路交战点附近，第4关敌人配置包含机器狗、无人机、飞机、油罐、坦克等带攻击范围的单位，因此会被附近玩家单位吸住反复攻击。

### 结论

当前表现不是第4关路径配置卡死，而是敌人攻击玩家单位时主动停下。若产品目标是普通敌人持续冲水晶，需要先调整设计：例如仅特定敌人可停下拆塔/打士兵，或给普通敌人增加“忽略玩家单位继续推进”的行为标记。

---

## 修复：第4关无人机群停在半路

### 现象

第4关无人机/飞行敌人在路径中段或合流点附近没有攻击动作，但停在原地不继续前进。

### 根因

`drone` 属于 `EnemyFlockMember` 鸟群/无人机群敌人，移动走 `processFlockEnemy`。该逻辑会给成员施加 `anchorOffsetX/Y` 编队偏移，让成员追随“路径点 + 偏移”的目标点；但路径推进判定只检查实体是否靠近原始路径点。当偏移距离大于 `FLOCK_PATH_ADVANCE_RADIUS` 时，成员会稳定停在偏移目标附近，却永远不满足“靠近原始 waypoint”的推进条件。

### 改动

- `MovementSystem.processFlockEnemy` 的路径推进判定同时接受原始 waypoint 与带编队偏移的目标点。
- 增加回归测试：LowAir 鸟群敌人靠近带偏移的编队目标点时必须推进 `pathIndex` 并能结算到达水晶。

### 验证

- `npm test -- src/systems/MovementSystem.test.ts` 通过，16 个用例。
- `npm run typecheck` 通过。

---

## 修复：弓手/法师在场时敌人原地停住

### 现象

场上存在弓手、法师等远程士兵后，敌人会停在路径中段，不继续前进，也没有明显攻击我方单位。

### 根因

敌人被位移到路径段外后会触发 `MovementSystem.tryPathRecovery`。旧逻辑恢复到最近 waypoint 后本帧直接 `continue`，把 `Movement.currentSpeed` 置为 0。远程士兵追击/站位会让敌人与路径恢复持续交替出现，导致敌人每帧都只恢复、不推进，视觉上表现为全体原地停住且没有攻击动作。

### 改动

- 路径恢复后刷新 `pathIndex` 和当前位置，并在同一帧继续执行路径推进。
- 更新 path recovery 回归测试：敌人被拉回路径后应立即继续移动，`currentSpeed` 保持大于 0。

### 验证

- `npm test -- src/systems/__tests__/MovementSystem.soldier.test.ts src/systems/MovementSystem.test.ts src/systems/SoldierAISystem.test.ts` 通过，73 个用例。
- `npm run typecheck` 通过。

---

## 修复：弓手攻击不播放攻击动作

### 现象

弓手进入战斗并发射远程投射物时，角色仍持续显示移动动作，没有切到 `attack_0/1` 攻击帧。

### 根因

弓手攻击走 `SoldierAISystem` 的士兵战斗逻辑。该逻辑在攻击冷却完成后只生成投射物或近战刀光，没有刷新 `Visual.attackAnimTimer`。`RenderSystem.getUnitSpriteState()` 只有在 `Visual.attackAnimTimer > 0` 时才会优先选择 `attack` 状态，因此弓手实际开火后仍按移动状态取图。

### 改动

- `SoldierAISystem` 新增士兵普攻动画触发逻辑，远程投射物、近战攻击和处决攻击都会设置 `Visual.attackAnimTimer/attackAnimDuration`。
- `SoldierAISystem.test.ts` 增加弓手远程攻击触发攻击动作计时器的回归测试。

### 验证

- `npm test -- src/systems/SoldierAISystem.test.ts` 通过，40 个用例。
- `npm run typecheck` 未通过，失败点为工作区既有 `src/systems/EnemySkillSystem.ts` 音效 key 字面量类型错误：`boss_phase_ice`、`boss_missile_impact`、`boss_devour` 不能赋给当前收窄到 `boss_devour_impact` 的类型。

---

## 功能：补充敌方单位与 Boss 专属音效

### 需求

补齐敌方单位和 Boss 缺少的专属音效，重点解决 Boss 出场、死亡、召唤、导弹、吞噬、阶段转换等事件过度共用通用音效的问题。

### 改动

| 文件/目录 | 改动 |
|-----------|------|
| `public/sfx/` | 新增 25 个 OpenGameArt CC0 OGG 音效文件，覆盖 Boss 出场/死亡/技能与敌人类型化出生/死亡 |
| `public/sfx/CREDITS.md` | 新增 SFX 来源记录，列出 OpenGameArt 包名、许可和运行时文件到原始文件的映射 |
| `src/utils/Sound.ts` | 扩展 25 个 SfxKey，支持 `SFX_PATH` 映射到 `.ogg`，路径归一化兼容 `.ogg` / `.wav` |
| `src/utils/audioKeys.ts` | 新增 Boss 类型、敌人类型、召唤技能、阶段技能到具体音效 key 的映射工具 |
| `src/core/RuleHandlers.ts` | `SFX_BOSS_SPAWN`、`SFX_BOSS_DIE`、`SFX_ENEMY_SPAWN`、`SFX_ENEMY_DIE` 按实体上下文播放差异化音效 |
| `src/systems/BossSystem.ts` | Boss 召唤、狂暴、导弹预警/落地、深渊吞噬施法/命中改为专属音效 |
| `src/systems/EnemySkillSystem.ts` | 数据驱动 Boss 技能按召唤/阶段/导弹/吞噬类型播放专属音效 |
| `src/core/RuleHandlers.test.ts` | 增加 Boss 类型映射、敌人类型映射回归测试 |
| `design/07-audio.md` | 更新音效资源清单、覆盖统计、资源来源和新增音效集成说明 |

### 验证

- `npm test -- src/core/RuleHandlers.test.ts` 通过，61 个用例。
- `npm run typecheck` 通过。
- 脚本校验 `src/utils/Sound.ts` 中 95 个 SFX 路径均存在实体文件。
- `file` 抽查新增 OGG：Ogg Vorbis 音频。

---

## 修正：确认新增音效来源

### 需求

新增的敌方单位与 Boss 音效必须来自开源音效库下载资源，而不是程序生成音效。

### 来源

- OpenGameArt `80 CC0 RPG SFX`
- OpenGameArt `50 CC0 Sci-Fi SFX`

### 验证

- 脚本校验 `src/utils/Sound.ts` 中 95 个 SFX 路径均存在实体文件，且新增敌方/Boss key 不再使用 WAV。
- `file` 抽查新增资源，确认替换后为 Ogg Vorbis 音频。
- `npm test -- src/core/RuleHandlers.test.ts` 通过，61 个用例。
- `npm run typecheck` 通过。

---

## 修复：嘲讽目标超出攻击范围时敌人停步

### 现象

敌人带有嘲讽状态时，如果嘲讽源仍存活但不在敌人的攻击范围内，旧逻辑会让敌人本帧停止移动；在战场拥挤或远程单位牵引场景下，容易表现为敌人停住但不攻击。

### 根因

`MovementSystem.processTauntTimer()` 只校验嘲讽源是否存活和计时是否有效，返回 true 后主流程无条件停止敌人移动。随后 `processEnemyAttack()` 如果发现嘲讽源超出攻击范围，会返回 false，但移动已经被跳过。

### 改动

- 嘲讽计时仍正常递减和锁定目标。
- 只有 `processEnemyAttack()` 真正执行攻击时，敌人才因嘲讽停步。
- 新增回归测试：被嘲讽但攻击距离不够的敌人继续沿路径移动。

### 验证

- `npm test -- src/systems/SkillSystem.test.ts src/systems/MovementSystem.test.ts src/systems/__tests__/MovementSystem.soldier.test.ts` 通过，36 个用例。
- `npm run typecheck` 通过。

---

## 检查：死亡动作播放链路

### 结论

死亡动作播放链路存在：单位死亡后，`LifecycleSystem` 会创建带 `DeathEffect` 的临时实体，并通过 `deathSpriteRegistry` 记录原单位的 art id；`RenderSystem.getUnitSpriteState()` 读取到该映射后会优先选择 `death` 状态，按 `DeathEffect.elapsed / duration` 切换 `death_0/1` 帧。

### 改动

- `DeathEffectSystem` 从 `eid = 0` 开始扫描死亡特效，避免 0 号实体上的死亡效果被漏计时。
- `LifecycleSystem.test.ts` 增加回归测试，确认普通敌人死亡效果继承原单位 `death` 动作资源，并在死亡效果结束后清理映射。

### 验证

- `npm test -- src/systems/LifecycleSystem.test.ts` 通过，7 个用例。
- `npm run typecheck` 通过。
- 资源检查：`public/art/units` 下 46 个单位均存在 `death_0` 和 `death_1`，缺失数为 0；弓手和哥布林死亡帧均存在。

---

## 检查：敌人攻击动作与死亡动作播放

### 结论

敌人攻击动作链路存在：`MovementSystem.processEnemyAttack()` 在普通敌人或 Boss 成功攻击玩家单位后，会设置 `Visual.attackAnimTimer`，`RenderSystem.getUnitSpriteState()` 随后优先选择 `attack` 状态。敌人死亡动作链路存在：敌人死亡后创建的 `DeathEffect` 临时实体继承原敌人的 `enemy_*` art id，渲染层选择 `death` 状态并播放 `death_0/1`。

### 改动

- `MovementSystem.test.ts` 增加普通敌人普攻玩家单位时触发攻击动作并停步的回归测试，避免只由 Boss 测试覆盖敌方攻击动画。

### 验证

- `npm test -- src/systems/MovementSystem.test.ts src/systems/LifecycleSystem.test.ts` 通过，24 个用例。
- `npm run typecheck` 通过。
- 资源检查：`public/art/units` 下 24 个敌人均存在 `attack_0/1` 和 `death_0/1`，缺失数为 0。

---

## 修复：盾卫单体嘲讽与敌人移动攻击

### 需求

盾卫嘲讽同一时间只影响 1 个敌人；被嘲讽敌人仅在盾卫处于自身攻击范围内时停止移动并攻击盾卫；盾卫死亡后嘲讽消失。普通敌人默认继续沿路径前进，遇到可攻击的我方单位时可以攻击但不停止移动。

### 改动

- 更新设计文档与盾卫技能描述，明确单体嘲讽、源死亡清除、普通敌人边移动边攻击。
- `SkillSystem` 的盾卫被动嘲讽改为优先维持当前目标，否则选择范围内最近的 1 个敌人，并清理同源多余嘲讽。
- `MovementSystem` 区分嘲讽/Boss 停步攻击与普通移动中攻击；普通敌人的非嘲讽攻击不再设置阻断移动的攻击动画计时。
- 补充回归测试覆盖单体嘲讽、盾卫死亡清嘲讽、普通敌人攻击时继续推进。

### 验证

- `npm test -- src/systems/SkillSystem.test.ts src/systems/MovementSystem.test.ts src/systems/__tests__/MovementSystem.soldier.test.ts` 通过，38 个用例。
- `npm run typecheck` 通过。

---

## 修复：非盾卫士兵误触发嘲讽

### 现象

弓手、法师等非盾卫士兵在场时也会触发盾卫嘲讽逻辑，导致敌人被错误挂载 `Taunted`。

### 根因

ECS `Skill.skillId` 使用 `ui8` 存储，旧映射把 `taunt` 编号设为 0；同时部署士兵时未知技能 id 使用 `?? 0` 兜底。弓手、法师等技能不在 `SkillSystem` 的主动技能映射中时，会被兜底为 0，并被反查为 `taunt`。

### 改动

- 将 `SkillSystem` 的技能编号调整为 `0 = none`、`1 = taunt`、`2 = whirlwind`、`3 = assassinate`。
- `SkillSystem.update()` 忽略 `none`，未知或未接入技能不再执行被动嘲讽。
- 部署士兵时只把明确支持的技能映射为对应编号，其余保持 `none`。
- 补充回归测试，确认非盾卫士兵的未知技能数字不会被误判为嘲讽。

### 验证

- `npm test -- src/systems/SkillSystem.test.ts src/systems/MovementSystem.test.ts src/systems/__tests__/MovementSystem.soldier.test.ts` 通过，39 个用例。
- `npm run typecheck` 通过。

---

## 调整：塔基础血量提升 10 倍

### 需求

将当前所有塔的基础血量提升到原来的 10 倍。

### 改动

- `src/config/units/towers.yaml` 中 10 个塔的 `stats.hp` 全部提升 10 倍。
- `src/data/gameData.ts` 的内置塔默认血量同步为同一组 10 倍数值，避免 YAML 注入前后的默认配置不一致。
- `design/06-technical.md` 中单位配置示例的塔血量同步为 10 倍。
- `configBridge.test.ts` 增加运行时断言，确认桥接后的塔血量会写入 `Health.current/max`。

### 验证

- `npm test -- src/data/levels/configBridge.test.ts` 通过，6 个用例。
- `npm run typecheck` 通过。

---

## 调整：激光塔持续攻击爬升速度

### 需求

激光塔持续攻击提升太慢，初始攻击改成 50%，并在 2 秒内提升到最大攻击力。

### 改动

- `AttackSystem` 将新激光束初始伤害从上限 10% 调整为 50%，并抽出 5 秒光束时长与 2 秒爬升时长常量。
- `LaserBeamSystem` 的 S 曲线爬升窗口改为 2 秒，2 秒后维持最大攻击力。
- 同步 `design/03-units.md`、`src/config/units/towers.yaml`、`src/data/gameData.ts` 中激光塔机制说明与 L1 DPS 参考。
- 补充激光塔单束生成与光束伤害爬升回归测试。

### 验证

- `npm test -- --run src/systems/AttackSystem.test.ts src/systems/LaserBeamSystem.test.ts` 通过，53 个用例。
- `npm run typecheck` 未通过，失败点为既有 `src/systems/RenderSystem.ts` 军阶图标常量缺失：`RANK_INSIGNIA_BLOCK_WIDTH`、`RANK_CHEVRON_THICKNESS`、`RANK_STAR_SIZE` 等，非本次激光塔改动范围。

---

## 调整：塔等级显示改为名称后缀

### 需求

去掉塔头顶的等级图标，改为在塔名称后追加罗马数字等级，例如 `箭塔[III]`。

### 改动

- `RenderSystem` 移除塔等级徽章绘制逻辑，塔名称统一通过 `formatTowerLevelDisplayName` 追加 `[I]` 到 `[V]` 后缀。
- 名称标签重新以塔位置居中绘制，不再为徽章预留横向布局空间。
- `design/05-presentation.md` 同步说明塔等级不再绘制额外图标。
- 补充渲染回归测试，覆盖 3 级塔显示为 `箭塔[III]`。

### 验证

- `npm test -- --run src/systems/RenderSystem.crystal.test.ts` 通过，7 个用例。
- `npm run typecheck` 通过；此前日志中记录的旧等级徽章常量缺失问题已随本次移除旧徽章逻辑解决。

---

## 调整：电塔跳跃次数与伤害成长

### 需求

提升电塔连锁跳跃次数：1 级跳跃 3 次，每次升级跳跃次数 +1，并同步提升伤害。

### 改动

- `design/03-units.md` 将电塔 L1-L5 调整为 3/4/5/6/7 次跳跃，伤害调整为 20/30/65/95/135。
- `src/config/units/towers.yaml` 同步电塔基础攻击、升级攻击成长、线性 `chainCountByLevel` 与被动描述。
- `src/data/gameData.ts` 同步内置兜底配置，避免 YAML 注入前后电塔攻击、攻速、射程、跳跃次数不一致。
- `AttackSystem` 注释改为说明链跳优先读取等级表。
- 补充回归测试，覆盖 YAML 桥接后的电塔配置、L1 实际最多命中 3 个目标、L5 实际最多命中 7 个目标。

### 验证

- `npm test -- --run src/systems/AttackSystem.test.ts src/data/levels/configBridge.test.ts` 通过，61 个用例。
- `npm run typecheck` 通过。

---

## 调整：5级电塔全屏落雷战略技能

### 需求

5级电塔增加全屏落雷技能：每10秒选择1个敌人造成高额闪电攻击；优先Boss和精英怪，没有高优先级目标时选择离我方水晶最近的敌人。同步补充卡牌技能说明和落雷表现设计。

### 改动

- `design/03-units.md` 增加 L5「天罚落雷」规则：10秒冷却、Boss/精英优先、否则按水晶距离选目标、900魔法伤害。
- `design/05-presentation.md` 补充落雷表现：天空压暗、白色闪光、粗闪电柱、冲击环、火花粒子、强屏幕震动。
- `src/config/units/towers.yaml` 与 `src/data/gameData.ts` 增加 `lightningStormCooldown` / `lightningStormDamage`，并补 L5 被动说明。
- `src/data/cards.ts` 与 `src/config/cards/towers.yaml` 更新电塔卡牌说明。
- 新增 `LightningStormSkill` / `LightningStorm` 组件和 `LightningStormSystem`，在 post-render 中绘制全屏压暗、白闪和落雷柱。
- `AttackSystem` 增加 5级电塔战略技能计时、全屏目标选择、900魔法伤害结算、ScreenShake 触发。
- `UnitFactory` 为电塔实体挂载战略技能冷却组件；升级到5级后立即进入可触发状态。

### 验证

- `npm test -- --run src/systems/AttackSystem.test.ts src/data/levels/configBridge.test.ts src/config/cards.test.ts` 通过，150 个用例。
- `npm run typecheck` 通过。

---

## 修复：黑暗牧师场景贴图回退成哥布林

### 需求

精英黑暗牧师在战场中显示为哥布林图片，需要查明原因并修复。

### 改动

- 根因：关卡和 YAML 已使用敌方黑暗牧师 ID `dark_priest`，但 `EnemyType` 仍保留旧值 `priest`；`WaveSystem` 写入 `UnitTag.unitTypeNum` 时找不到对应类型，渲染反查失败后回退到 `goblin`。
- 将敌人枚举同步为 `EnemyType.DarkPriest = 'dark_priest'`，并更新内置兜底配置。
- 在 `gameData.ts` 集中导出 `ENEMY_TYPE_BY_ID` / `ENEMY_ID_BY_TYPE`，替换 `WaveSystem`、`RenderSystem`、`LifecycleSystem`、`BossSystem` 中重复维护的敌人类型表。
- 同步 `design/03-units.md` 中黑暗牧师 ID，保持与 `design/04-levels.md` 和 YAML 配置一致。
- 补充波次生成回归测试，覆盖黑暗牧师实体类型编号可反查回 `dark_priest`，避免再次回退成哥布林贴图。

### 验证

- `npm test -- src/systems/__tests__/WaveSystem.spawn.test.ts src/config/levels.test.ts src/utils/artAtlasBuild.test.ts` 通过，40 个用例。
- `npm run typecheck` 通过。

---

## 检查：运行时单位类型映射一致性

### 需求

检查除黑暗牧师外，代码和配置中是否还有类似“配置 ID 与运行时类型映射不一致，导致贴图回退”的问题。

### 结论

- 当前 5 个关卡实际引用的 24 个敌人都能映射到 `EnemyType` / `ENEMY_ID_BY_TYPE`，并且都有 `public/art/units/unit_enemy_*_idle_0.png` 场景贴图。
- 当前卡牌实际引用的 20 个士兵、塔、机关都能映射到运行时类型编号，并且都有对应场景贴图。
- 配置全集中存在一批未被关卡或卡牌引用的历史/草稿敌人 ID（如 `grunt`、`runner`、`e_*`、`boss_*`），它们不在当前枚举和资源集中；由于当前运行时不引用，不会触发黑暗牧师同类问题。

### 改动

- 新增 `src/config/unitRuntimeMapping.test.ts`，固化运行时 guardrail：
  - 关卡波次敌人必须存在于 `EnemyType`，并能通过 `ENEMY_ID_BY_TYPE` / `ENEMY_TYPE_BY_ID` 双向反查。
  - 关卡波次敌人必须有场景贴图。
  - 卡牌引用的士兵、塔、机关必须能映射到对应运行时类型编号，并有场景贴图。

### 验证

- `npm test -- src/config/unitRuntimeMapping.test.ts src/config/levels.test.ts src/config/cards.test.ts` 通过，95 个用例。
- `npm run typecheck` 通过。

---

## 重设计：胜利/失败结算界面仪式感

### 需求

胜利/失败界面删除场景棋盘，只保留背景图；UI 背景半透明且更有仪式感；胜利标题不再显示“恭喜通关”，改为章节结尾句；胜败剧情文字以史诗感浮现；展示战斗统计信息；修复胜利、失败 BGM 未稳定播放的问题。

### 改动

- `design/04-levels.md` 与 `design/05-presentation.md` 更新结算界面事实来源：胜败复用同一全屏覆盖层，背景保留主题图，UI 面板 85% 不透明，章节句 + 剧情打字机 + 统计信息。
- 重写 `VictoryScreenSystem` 为胜败共用结算覆盖层：支持主题背景图、章节句弹性浮现、胜利星级和彩带、失败暗红光效、剧情文字逐字出现、统计卡片依次出现。
- `main.ts` 在结算前快照统计：波次、敌人、使用塔、兵数量、水晶血量、总得分、总伤害。
- 累计对敌总伤害与击败敌人数量，供结算统计使用。
- 修正阶段切换时从部署阶段恢复关卡主题 BGM，避免回到 `battle_default`；胜利/失败处理显式切换到对应结算 BGM。

### 验证

- `npm run typecheck` 通过。

- `npm test -- --run src/systems/UISystem.test.ts src/config/levels.test.ts` 通过，22 个用例。
- `npm run build` 通过，仅保留既有 Vite chunk size 警告。
- 浏览器冒烟：本地 `http://127.0.0.1:3000/ai-tower-defender/` 中进入第 1 关，调试触发胜利与失败结算，画面正常显示且控制台无 error。

---

## 优化：手牌抽卡同类上限

### 需求

如果手牌中的某种单位大类（兵、塔、法术）同类型超过 2 个，不能继续抽同类型卡。

### 改动

- `design/02-gameplay.md` 将手牌规则更新为兵、塔、法术三类各自最多 2 张，并保留同一张卡最多 2 张的限制。
- `HandSystem` 抽牌判断从“同卡重复数”扩展为“同卡重复数 + 受控大类数量”，覆盖初始抽牌、手动抽牌和出牌后自动补牌。
- 大类判定沿用现有卡牌结构：`spell` 为法术，`unit` 且 `card_*_tower` 为塔，其他 `unit` 为兵；机关不纳入兵/塔/法术同类上限，只保留同卡重复限制。
- 调整 `HandSystem`、`CardDraftSystem`、`DebugManager` 测试数据，避免测试起手牌违反新的同类上限，并新增同类上限回归用例。

### 验证

- `npm test -- src/debug/DebugManager.test.ts src/systems/__tests__/CardDraftSystem.test.ts src/systems/__tests__/HandSystem.test.ts` 通过，74 个用例。
- `npm run typecheck` 通过。
- `npm test` 通过，90 个测试文件、1422 个用例。

---

## 调整：暴风雪改为全屏持续技能

### 需求

暴风雪范围改成全屏技能，持续时间改成5秒，伤害提升，金币花费提高。

### 改动

- `design/03-units.md`、`design/02-gameplay.md`、`design/塔防游戏设计大纲.md` 同步暴风雪规则：点击即全局释放，全棋盘覆盖，造成 45 点魔法伤害并施加 5 秒减速。
- `src/config/cards/spells.yaml` 将暴风雪改为 `placement.targetType=global`，金币费用从 55 提高到 90，伤害从 15 提高到 45，保留 5 秒减速。
- `src/ui/LayoutConstants.ts` 将暴风雪纳入自施法技能卡，点击手牌即释放，不再拖拽选点。
- `src/main.ts` 暴风雪释放改为调用全局法术效果。
- `src/systems/SpellProjectileSystem.ts` 支持暴风雪全屏 5 秒冷雾覆盖、全敌人一次性伤害与减速，并避免继承大地裂变的长时间强震屏。
- 补充配置和运行时回归测试，覆盖全屏暴风雪伤害、减速、持续 5 秒以及不重复伤害。

### 验证

- `npm test -- src/config/cards.test.ts src/systems/SpellProjectileSystem.test.ts` 通过，89 个用例。
- `npm run typecheck` 通过。

---

## 调整：法术卡拖拽只显示释放半径

### 需求

法术卡牌拖动的时候不要显示“法术”文字，直接显示攻击半径。

### 改动

- `UISystem.buildDragGhost` 对法术拖拽 ghost 改为范围预览专用：绘制配置中的 `spellEffect.radius` 后立即返回，不再绘制文字或简单圆形主体。
- `design/05-presentation.md` 补充法术卡拖拽表现规范：不显示“法术”或卡名，只显示释放攻击半径。
- 新增 `UISystem` 回归测试，断言火球术拖拽显示 80px 半径圈且不会生成“法术”/卡名 label。

### 验证

- `npm test -- src/systems/UISystem.test.ts` 通过，14 个用例。
- `npm run typecheck` 通过。

---

## 调整：大地裂变反馈与敌我伤害

### 需求

大地裂变的棋盘震动幅度减少，持续期间增加地震音效，增加屏幕震动效果，并且技能对我方单位也造成伤害。

### 改动

- `design/03-units.md` 明确大地裂变敌我双方生效、地格轻微抖动、持续地震音效和完整屏幕震动表现。
- `design/07-audio.md` 新增 `skill_earthquake` 技能卡音效并更新覆盖统计。
- `SpellProjectileSystem` 将大地裂变地格抖动幅度减半，屏幕震动调整为持续 3 秒、强度 12、频率 20，并改用 `skill_earthquake`。
- `SpellProjectileSystem` 的大地裂变伤害目标从仅敌人扩展为所有有生命单位，保留其他法术只伤害敌人的行为。
- `Sound.ts` / `SoundSynth.ts` 新增 `skill_earthquake` 合成音效回退，避免缺少实体 OGG 时静默无声。
- `SpellProjectileSystem.test.ts` 增加回归断言：友方单位每秒受伤、地格抖动降低、屏幕震动参数正确、地震音效被触发。

### 验证

- `npm test -- src/systems/SpellProjectileSystem.test.ts` 通过，4 个用例。
- `npm run typecheck` 通过。

---

## 修正：全屏法术拖拽到棋盘内释放

### 需求

全屏法术卡必须拖动到棋盘范围内释放鼠标才生效，不要点击立即生效。

### 改动

- `LayoutConstants` 只保留 `gold_rush` 为点击立即生效的自施法技能；暴风雪和大地裂变改回普通法术拖拽流程。
- `main.ts` 在法术卡释放时增加棋盘矩形校验，鼠标释放点不在棋盘范围内时提示“超出地图范围”、播放拒绝音效并取消拖拽，不扣金币也不消耗手牌。
- `design/03-units.md` 将暴风雪和大地裂变的投射/预警阶段更新为“从手牌拖到棋盘范围内释放才触发”。
- `LayoutConstants.test.ts` 增加回归测试，防止全屏伤害法术再次被归入点击自施法列表。

### 验证

- `npm test -- src/ui/LayoutConstants.test.ts` 通过。
- `npm run typecheck` 通过。

---

## 修复：机关抽卡只出现捕兽夹

### 需求

玩家反馈机关牌现在只有捕兽夹，每次抽卡都是捕兽夹。

### 诊断

- 关卡 YAML 已配置 `cardPool` / `draftPool`，第 1 关抽卡池包含地刺、捕兽夹、焦油坑、巨石。
- 运行时 `LevelConfig` 没有承接这两个字段，`main.ts` 启动战斗时仍按关卡号使用硬编码卡池。
- 第 3 关硬编码卡池里唯一机关是 `card_bear_trap`，因此出牌自动补牌时会反复出现捕兽夹。

### 改动

- `LevelFormModel` / `LevelConfig` 增加 `cardPool`、`draftPool` 字段。
- `parseYamlToModel` 和 `modelToLevelConfig` 显式解析并传递关卡卡池。
- `main.ts` 战斗初始化优先按关卡 YAML 的卡牌 ID 解析卡池，旧硬编码卡池只作为兜底。
- 将 `draftPool` 注册进 `HandSystem` 卡库，使出牌后的自动补牌使用关卡抽卡池。
- 补充 level model 与 modelToLevelConfig 测试，覆盖卡池字段解析和运行时映射。

### 验证

- `npm run typecheck` 通过。
- `npm test -- src/editor/__tests__/levelModel.test.ts src/editor/state/__tests__/modelToLevelConfig.test.ts src/systems/__tests__/HandSystem.test.ts` 通过。

---

## 增强：捕兽夹持久控制与冷却

### 需求

进入捕兽夹的敌人会被困住2秒，捕兽夹不消失，但有5秒冷却。

### 改动

- `TrapSystem` 将捕兽夹从一次性销毁改为持久机关：触发后添加 `Stunned` 2秒、造成20点物理伤害、进入5秒冷却，不再 `destroyEntity`。
- 冷却期间捕兽夹不重复触发，冷却结束后可以再次困住进入同格的非 Boss 敌人。
- `src/config/units/traps.yaml` 与 `src/data/gameData.ts` 同步捕兽夹 `cooldown: 5.0`、`maxTriggers: 0`。
- 更新捕兽夹卡牌描述和 `design/03-units.md`，移除“一次性/触发后消失/1秒”的旧描述。
- `TrapSystem.test.ts` 增加回归覆盖：触发后不销毁、5秒冷却、冷却期间不重复触发、冷却结束可再次触发。

### 验证

- `npm test -- src/systems/TrapSystem.test.ts src/data/levels/configBridge.test.ts src/config/cards.test.ts` 通过。
- `npm run typecheck` 通过。

---

## 优化：抽卡权重衰减

### 需求

出现过的卡随机权重会降低；卡池所有卡初始随机权重为 20，抽到一次降低 1 点，最低降低到 5。

### 改动

- `design/02-gameplay.md` 在手牌区规则中补充卡池随机权重说明，明确成功抽入手牌后降低自身权重。
- `HandSystem` 新增每局卡牌权重表，初始化和扩库时为卡牌设置 20 点初始权重。
- 初始手牌、手动抽牌、出牌后自动补牌在成功抽入手牌后都会消耗对应卡牌 1 点权重，最低保持 5。
- 出牌后自动补牌从等概率随机改为按当前权重加权抽取，降低已频繁出现卡牌的再次出现概率。
- `HandSystem.test.ts` 增加权重衰减下限和加权随机区间选择回归测试。

### 验证

- `npm test -- src/systems/__tests__/HandSystem.test.ts` 通过。
- `npm run typecheck` 通过。

---

## 功能：HUD 金手指金币按钮

### 需求

在金币图标旁增加金手指按钮，点击随机增加1-2金币，并显示 `+N💰` 飘字。

### 改动

- `design/05-presentation.md` 补充顶部 HUD 金手指按钮的位置、表现和飘字反馈。
- `design/02-gameplay.md` 将金手指按钮列入战斗内临时调试金币来源。
- `UISystem` 在战斗阶段、金币数右侧显示小号 `+` 按钮，通过回调触发加金币逻辑。
- `main.ts` 实现金手指回调：随机增加1-2金币，播放金币获得音效，并在金币 HUD 附近生成 `+N💰` 飘字。
- `UISystem.test.ts` 增加按钮位置、点击回调、非战斗阶段隐藏的回归测试。

### 验证

- `npm run typecheck` 通过。
- `npx vitest run src/systems/UISystem.test.ts` 通过。

---

## 调整：金手指按钮图标

### 需求

将金币金手指按钮图标从 `+` 改为手指。

### 改动

- `UISystem` 将金手指按钮标签改为 `☝`。
- `UISystem.test.ts` 同步按钮查询断言。
- `design/05-presentation.md` 同步顶部 HUD 规格描述。

### 验证

- `npx vitest run src/systems/UISystem.test.ts` 通过。

---

## 修复：结算面板标题重复与统计越界

### 需求

结算界面顶部标题与正文小标题重复显示，底部统计 UI 超过面板底框，需要修正布局。

### 改动

- `design/05-presentation.md` 明确章节结尾句是结算面板唯一标题，正文区域不重复显示同名小标题。
- `VictoryScreenSystem` 去掉剧情正文小标题，只保留顶部章节句。
- 结算面板尺寸、分割线、正文和统计区抽成统一常量，统计项改为基于面板安全区计算位置。
- 统计区整体上移，第三行统计保留底部安全间距，避免贴近或越过底框。

### 验证

- `npm run typecheck` 通过。

---

## 修复：巨石阻挡地面敌人

### 需求

巨石机关应作为地面路障阻挡地面敌人前进，敌人需要攻击并破坏巨石后才能继续沿路径移动；低空敌人不受地面巨石阻挡。

### 改动

- `MovementSystem` 新增存活 Ground 巨石查询，地面敌人路径推进时若本帧移动段与巨石碰撞则保持原位置、原路径进度并停步。
- 敌人普通攻击目标搜索补充可攻击巨石，确保被巨石阻挡时会持续攻击巨石。
- `MovementSystem.test.ts` 增加巨石阻挡、破坏后放行、低空敌人不受阻挡的回归测试。

### 验证

- `npm run typecheck` 通过。
- `npm test -- MovementSystem.test.ts` 通过。
- `npm test -- TrapSystem.test.ts` 通过。
- `npm test` 通过，90 个测试文件、1434 个用例全部通过。

---

## 修复：巨石运行时血量防御映射

### 需求

巨石机关应从配置读取血量、防御和魔抗，否则无法作为可被攻击破坏的路障参与战斗。

### 改动

- `TrapConfig` 补充 `defense` 和 `magicResist` 字段。
- `trapBridge` 将 YAML `stats.armor` / `stats.mr` 映射到运行时 `defense` / `magicResist`。
- `configBridge.test.ts` 增加巨石机关桥接回归测试，验证创建出的巨石拥有 `Health.current/max/armor/magicResist`。

### 验证

- `npm run typecheck` 通过。
- `npm test -- src/data/levels/configBridge.test.ts MovementSystem.test.ts` 通过，28 个用例。

---

## 修复：巨石受击反馈

### 需求

敌人攻击巨石时需要播放攻击动作，巨石作为可破坏路障需要显示血条。

### 改动

- `MovementSystem` 将敌人攻击动作与停步逻辑解耦：普通敌人移动中攻击也会设置 `Visual.attackAnimTimer`，但仍可继续沿路径移动。
- `RenderSystem` 机关主体绘制后不再提前退出，保留通用血条叠加层；巨石等有 `Health` 的机关会显示血条。
- `MovementSystem.test.ts` 补充普通敌人攻击动作与巨石目标攻击动作断言。
- `RenderSystem.crystal.test.ts` 增加巨石主体绘制后仍显示血条的回归测试。

### 验证

- `npm run typecheck` 通过。
- `npm test -- MovementSystem.test.ts RenderSystem.crystal.test.ts` 通过，29 个用例。
- `npm test` 运行到 88/90 个测试文件通过；失败 2 个为当前工作区已有抽卡/防空相关未提交改动导致的 `CardDraftSystem.test.ts`、`HandSystem.test.ts` 断言失败，和本次移动/渲染改动无关。

---

## 修改：冰塔火塔毒塔可攻击低空单位

### 需求

冰塔、火塔、毒塔都可以攻击 LowAir 低空单位。

### 改动

- `design/03-units.md` 和 `design/04-levels.md` 同步低空攻击规则，明确冰塔、火塔、毒塔属于稳定 LowAir 对策。
- `lowAirTargeting` 将冰塔、火塔、毒塔及其卡牌加入 LowAir 白名单。
- `gameData` 静态塔配置同步 `canTargetLowAir: true`，避免静态配置和 YAML 桥接路径行为不一致。
- `AttackSystem.test.ts` 增加白名单回归测试。
- `HandSystem.test.ts` 调整防空保底用例，保持“前 5 张没有防空牌”语义。

### 验证

- `npm test -- src/systems/AttackSystem.test.ts src/systems/__tests__/HandSystem.test.ts` 通过，2 个测试文件、99 个用例。
- `npm run typecheck` 未通过，阻断点为当前工作区已有的 `src/systems/UISystem.test.ts:569` 中 `placement.range: 'global'` 与 `CardPlacementRange` 类型不匹配；该文件不属于本次改动。

---

## 修改：暴风雪拖拽释放与满屏飞雪

### 需求

暴风雪卡牌需要拖动到场景中，拖动期间预览全屏攻击范围，鼠标放开后才生效；释放后能看到持续 5 秒的满屏飞雪。

### 改动

- `UISystem` 为全屏法术增加棋盘范围预览，暴风雪拖动时显示整块棋盘覆盖框，不再显示跟随鼠标的大圆。
- `SpellProjectileSystem` 增强暴风雪持续特效，增加全屏冷雾、斜向飞雪、风线和旋转冰晶密度，保留首帧伤害与减速、持续 5 秒不重复结算伤害。
- `UISystem.test.ts` 和 `SpellProjectileSystem.test.ts` 增加暴风雪拖拽预览、满屏粒子密度和 5 秒持续性的回归测试。

### 验证

- `npm run typecheck` 通过。
- `npm test -- src/systems/UISystem.test.ts src/systems/SpellProjectileSystem.test.ts src/ui/LayoutConstants.test.ts` 通过。

---

## 优化：抽卡连续去重

### 需求

连续两次抽卡不能抽到重复的卡。

### 改动

- `design/02-gameplay.md` 在手牌区规则中补充连续抽牌限制。
- `HandSystem` 记录上一次成功抽入手牌的卡牌 ID。
- 随机补牌候选池存在其他合法卡牌时，临时排除上一张卡，避免连续两次随机抽到同一张。
- 若当前只有上一张卡是唯一合法候选，则允许兜底抽取，避免抽牌卡死。
- `HandSystem.test.ts` 增加连续去重和唯一候选兜底回归测试。

### 验证

- `npm test -- src/systems/__tests__/HandSystem.test.ts` 通过，41 个用例。
- `npm run typecheck` 通过。

---

## 修改：弓手法师可攻击低空单位

### 需求

弓手、法师都可以攻击 LowAir 低空单位。

### 改动

- `design/03-units.md` 和 `design/04-levels.md` 同步低空攻击规则，明确法师属于稳定 LowAir 对策；弓手保持对空能力。
- `lowAirTargeting` 将法师和 `card_mage` 加入 LowAir 白名单。
- `gameData` 将法师静态配置同步为 `canTargetLowAir: true`。
- `AttackSystem.test.ts` 增加弓手、法师 LowAir 白名单回归测试。

### 验证

- `npm test -- src/systems/AttackSystem.test.ts` 通过，61 个用例。
- `npm run typecheck` 通过。

---

## 修改：暴风雪左向右击退眩晕

### 需求

暴风雪视觉上表现为从左向右的风吹雪效果，生效时所有敌人向右位移 1 格并眩晕 1 秒。

### 改动

- `design/03-units.md` 更新暴风雪技能卡规则和粒子表现，明确左向右风雪、右推 1 格、眩晕 1 秒。
- `SpellProjectileSystem` 将暴风雪控制效果从减速改为向右推 64px 并添加 `Stunned` 1 秒，右边界处限制到棋盘内。
- 暴风雪雪粒和风线改为横向从左到右扫过，强化风向一致性。
- `SpellProjectileSystem.test.ts` 更新回归测试，覆盖右推、边界夹取、敌人眩晕、友方不受影响和横向风线。

### 验证

- `npm test -- src/systems/SpellProjectileSystem.test.ts` 通过。
- `npm run typecheck` 通过。

---

## 核查：路西法技能设计与程序释放

### 结论

路西法技能设计为“召唤骷髅大军”，程序侧已实现自动释放。

### 核查结果

- `design/03-units.md` 明确路西法为混乱阵营 Boss，每 10 秒在周围召唤 3 个骷髅，场上骷髅上限 12；HP 低于 30% 时召唤间隔缩短为 5 秒，ATK +50%。
- `src/config/units/enemies.yaml` 中 `lucifer` 已配置 `bossType: Lucifer` 和 `summon_skeletons` 技能。
- `BossSystem.handleLucifer` 会根据 Boss 计时器自动触发召唤，暴怒时缩短间隔并提升攻击力。
- `BossSystem.spawnLuciferSkeletons` 会创建混乱阵营骷髅实体，属性为 HP 60、ATK 8、移速 50，并限制总数不超过 12。
- `BossSystem.test.ts` 已覆盖 10 秒召唤 3 个骷髅、12 个骷髅上限、低血量暴怒缩短到 5 秒并 ATK +50%。

### 验证

- 本次为代码阅读核查，未改动业务代码，未运行测试。

---

## 修改：暴风雪明显雪花强化

### 需求

暴风雪需要大量、明显、可辨识的雪花效果。

### 改动

- `SpellProjectileSystem` 在原有左向右风雪上新增大雪花层，每个雪花由亮色菱形核心和横竖线组成，覆盖全屏横向扫过。
- `SpellProjectileSystem.test.ts` 增加大雪花数量与亮色雪花线条的回归断言，确保不是只有小粒子。

### 验证

- `npm test -- src/systems/SpellProjectileSystem.test.ts` 通过。
- `npm run typecheck` 通过。

---

## 修复：卡牌点击不直接生效

### 需求

卡牌需要拖拽到场景中释放才生效，单纯点击手牌不能立即生效。

### 改动

- `main.ts` 将手牌按下改为拖拽候选状态，移动超过阈值后才启动 `BuildSystem` 拖拽。
- 移除技能卡点击即时生效路径，所有技能卡都必须拖拽到棋盘内松开才执行。
- `LayoutConstants.test.ts` 扩展技能卡非自施法断言，覆盖火球、剑雨、暴风雪、炸弹、大地裂变和淘金热。

### 验证

- `npm run typecheck` 通过。
- `npm test -- src/ui/LayoutConstants.test.ts src/systems/UISystem.test.ts` 通过。
