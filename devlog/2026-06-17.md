# 2026-06-17 开发日志

## 修改：暴风雪持续伤害与全局停步

### 需求

暴风雪持续期间，每秒造成一次伤害，并使所有单位停止移动。

### 改动

- `design/03-units.md` 将暴风雪从首帧伤害、位移、眩晕改为 5 秒持续法术：每秒 45 魔法伤害，共 5 次，持续期间所有单位停止移动。
- `SpellProjectileSystem` 对暴风雪按持续效果 tick 结算伤害：效果出现即结算第 1 次，之后每秒结算一次，不再推开或眩晕敌人。
- `MovementSystem` 增加活动暴风雪检测，暴风雪存在期间统一阻止敌人路径移动和士兵移动，并把当前速度置 0。
- `SpellProjectileSystem.test.ts` 与 `MovementSystem.soldier.test.ts` 增加回归覆盖，验证暴风雪持续伤害、不位移不眩晕，以及敌我可移动单位停步。

### 验证

- `npm test -- src/systems/SpellProjectileSystem.test.ts src/systems/__tests__/MovementSystem.soldier.test.ts` 通过。
- `npm run typecheck` 通过。

## 修改：暴风雪混乱攻击目标

### 需求

暴风雪对我方士兵也造成伤害（混乱攻击），但是对塔没有伤害。

### 改动

- `design/03-units.md` 补充暴风雪混乱攻击规则：伤害敌人和我方士兵，不伤害塔。
- `SpellProjectileSystem` 为暴风雪增加专属目标过滤，保留敌人受伤，新增我方 `Soldier` 受伤，并显式排除 `Tower`。
- `SpellProjectileSystem.test.ts` 覆盖我方士兵持续受伤、我方塔不受伤的回归场景。

### 验证

- `npm test -- src/systems/SpellProjectileSystem.test.ts` 通过。
- `npm run typecheck` 通过。

## 调整：路西法骷髅翻土特效颜色

### 需求

路西法召唤骷髅时的地下钻出翻土特效应表现为黑色土，而不是土黄色尘土。

### 改动

- `BossSystem` 将骷髅钻出 `BurrowTrail` 粒子颜色调整为黑色土色值 `38,34,30`。
- `BossSystem.test.ts` 同步更新尘土粒子颜色断言，避免回归到土黄色。

### 验证

- `npm test -- src/systems/BossSystem.test.ts` 通过。
- `npm run typecheck` 通过。

## 任务：核对第四关 Boss 技能实现状态

时间：2026-06-17 09:06:57 CST

### 结论

- 第四关 Boss 是 `super_robot`（超级机器人），出现在 `src/config/levels/level-04.yaml` 第 8 波 Boss 波。
- 配置技能为 `targeted_missile`（远程导弹轰炸）：每 10 秒选择目标点，2 秒预警，造成中心 80、半径 80px 递减伤害。
- 运行时代码已实现：
  - `src/systems/BossSystem.ts` 中 `BossType.SuperRobot` 走专用导弹逻辑，包含塔密集区域选择、`TargetingMark` 预警、导弹爆炸、震屏和音效。
  - `src/systems/EnemySkillSystem.ts` 中也注册了 `targeted_missile` handler，用于数据驱动技能。
  - `src/systems/WaveSystem.ts` 会把 `super_robot` 挂载为 `BossType.SuperRobot` 并注册技能实体。
- 当前实际主流程中 `BossSystem` 已注册到 `main.ts` 系统管线，因此超级机器人的专用导弹逻辑会执行。

### 核对文件

- `src/config/levels/level-04.yaml`
- `src/config/units/enemies.yaml`
- `src/systems/WaveSystem.ts`
- `src/systems/BossSystem.ts`
- `src/systems/EnemySkillSystem.ts`
- `src/systems/BossSystem.test.ts`
- `src/config/enemySkills.test.ts`

### 备注

- 本次未修改功能代码。
- 工作区已有用户改动：`src/core/components.ts`，本次未触碰。

---

## 修改：虫族女王虫群孵化与塔免疫

### 需求

虫族女王 `虫群孵化` 改为每 10 秒召唤 5 只随机虫族单位；修复塔免疫未阻止塔锁定 Boss 的问题；Boss 技能释放时需要在棋盘上方飘字显示技能名和描述，持续 10 秒后消失。

### 改动

- `design/03-units.md` 与 `src/config/units/enemies.yaml` 同步虫群孵化规则：10 秒、5 只、随机虫族单位池。
- `BossSystem` 将 QueenWorm 召唤池扩展为沙漠黑虫、钻地甲虫、吸血蝗虫、自爆甲虫，并在释放时触发技能提示。
- `AttackSystem.isValidTarget` 对塔攻击者读取 `Boss.immuneToTowers`，塔无法锁定虫族女王；非塔攻击者仍可攻击。
- 新增 `BossSkillAnnouncementSystem` 与 `BossSkillAnnouncement` 组件，负责 Boss 技能提示的 10 秒生命周期和棋盘上方绘制。
- `main.ts` 接入 Boss 技能提示系统更新与渲染。

### 验证

- `npm test -- BossSystem.test.ts AttackSystem.test.ts BossSkillAnnouncementSystem.test.ts` 通过。
- `npm run typecheck` 通过。

---

## 修复：虫族女王召唤初始化与攻击动作

### 问题

- 虫族女王召唤的随机虫族单位是 `BossSystem` 手工创建实体，缺少 `UnitTag.unitTypeNum` 等完整初始化字段，渲染层按默认 `Goblin` 兜底，导致钻地甲虫显示成哥布林形象。
- 虫族女王攻击盾卫时需要明确触发攻击动作并停步。

### 改动

- `BossSystem` 的 QueenWorm 召唤池补充真实 `EnemyType` 和攻击动画时长，召唤实体写入 `unitTypeNum`、完整 `Visual` 动画字段。
- `BossSystem.test.ts` 增加召唤物真实敌人类型断言，防止回退到哥布林默认形象。
- `MovementSystem.test.ts` 增加虫族女王攻击盾卫的回归测试，验证目标锁定、投射物生成、攻击动画计时和 Boss 停步。

### 验证

- `npm test -- BossSystem.test.ts MovementSystem.test.ts AttackSystem.test.ts BossSkillAnnouncementSystem.test.ts` 通过。
- `npm run typecheck` 通过。

---

## 修复：路西法骷髅召唤后瞬移

### 问题

路西法召唤骷髅时，骷髅初始位置在路西法周围，但 `Movement` 组件仍使用 `pathIndex=0, progress=0, spawnIdx=0`，下一帧移动系统会按出生路径起点重算坐标，表现为骷髅瞬移到出生口。

### 改动

- `BossSystem` 在创建路西法骷髅时，使用 `MovementSystem.findNearestPathLocation` 把骷髅落点映射到最近路径段进度。
- 为骷髅补充 `EnemyFlockMember`，使用召唤点相对路径投影点的偏移保持分散落位，同时沿路径推进。
- `MovementSystem.findNearestPathLocation` 返回最近路径投影坐标，供召唤物初始化偏移使用。
- `BossSystem.test.ts` 增加回归测试，覆盖骷髅移动一帧后不会瞬移回出生口。

### 验证

- `npm test -- src/systems/BossSystem.test.ts` 通过。
- `npm test -- src/systems/MovementSystem.test.ts src/systems/__tests__/MovementSystem.soldier.test.ts` 通过。
- `npm run typecheck` 未通过；剩余错误来自工作区已有的超级机器人导弹改动：`BossSystem.test.ts` 黑烟断言可能为 undefined、`BossSystem.ts` 使用不存在的 `MapConfig.width`。

---

## 修复：深渊领主黑暗吞噬保留单一路径

时间：2026-06-17 09:32:45 CST

### 问题

深渊领主 `dark_devour` 同时存在两套运行路径：

- `BossSystem` 中的硬编码 `AbyssLord` 分支。
- `EnemySkillSystem` 中读取 YAML `skills` 的配置驱动实现。

文档要求敌人技能由 `skills` 配置声明，并由 `EnemySkillSystem` 创建 `EnemySkillParticleEffect` 统一播放，因此配置驱动路径更接近设计。

### 改动

- `BossSystem` 移除深渊领主周期吞噬硬编码分支，避免与 YAML 技能重复。
- `EnemySkillSystem` 放开 `BossType.AbyssLord` 进入配置驱动 Boss 技能路径。
- `dark_devour` 结算时排除其他 Boss 与基地目标，吞噬普通单位后置 0 血量并标记延迟移除。
- `EnemySkillSystem.test.ts` 增加深渊领主黑暗吞噬回归测试，覆盖范围、回血、其他 Boss/基地排除和粒子创建。
- `design/03-units.md` 更新 Boss 技能配置审查结论，修正 `abyss_lord` 仍缺 `skills` 的过期描述。
- `Boss.abilityTimer` 注释移除 AbyssLord，避免后续误认为仍由 `BossSystem` 计时。

### 验证

- `npm test -- src/systems/EnemySkillSystem.test.ts src/systems/BossSystem.test.ts src/config/enemySkills.test.ts` 通过。
- `npm run typecheck` 通过。

---

## 功能：超级机器人导弹实体化

时间：2026-06-17 09:32:29 CST

### 需求

超级机器人发射导弹时需要从机器人身上生成导弹实体，导弹以抛物线高速落到我方单位区域；导弹实体为铁锈色，带淡黄色尾焰；超级机器人攻击距离为1/3棋盘宽度；导弹爆炸后冒出黑色烟雾。

### 改动

- `design/03-units.md`、`src/config/units/enemies.yaml` 同步更新超级机器人导弹技能规则和表现说明。
- `BossSystem` 将超级机器人导弹从直接结算改为：2秒预警后从Boss当前位置生成 `Projectile` 实体，并限制目标选择在1/3棋盘宽度内；目标评分按塔密度优先、士兵密度次选，场上只有士兵时也会选择我方单位区域。
- `ProjectileSystem` 增加超级机器人导弹落地分支，复用抛物线飞行，落地后对我方阵营单位做范围递减伤害并生成黑色烟雾。
- `RenderSystem` 为超级机器人导弹增加专属铁锈色弹体和淡黄色尾焰，不影响导弹塔蓝色尾焰外观。
- 新增 `projectileTypes.ts` 保存 Boss 导弹投射物源类型常量，避免系统间为了常量产生循环依赖。
- `BossSystem.test.ts` 增加导弹实体、落地伤害、黑烟、士兵目标、1/3棋盘宽度攻击范围回归覆盖。

### 验证

- `npm test -- src/systems/BossSystem.test.ts src/systems/ProjectileSystem.test.ts` 通过。
- `npm test -- src/config/enemySkills.test.ts src/config/levels.test.ts` 通过。
- `npm run typecheck` 通过。

---

## 功能：弩塔弩矢视觉与飞行音效重绘

时间：2026-06-17 10:15:20 CST

### 需求

弩塔子弹需要重新绘制为 60px 长弩矢，带蓝色外发光、破空特效，并补充飞行音效；音效资源必须使用开源音效。

### 改动

- `design/05-presentation.md` 补充 `ballista bolt` 弹道表现：60px 长弩矢、白到蓝渐变、蓝色软发光、两条浅蓝破空线，并保持锁定发射方向。
- `design/07-audio.md` 更新 `tower_ballista` 的设计意图，注明使用短促破空呼啸表现高伤穿透感。
- `Renderer` 的 `arrow` 形状新增 `arrowGlowColor`、`arrowGlowAlpha`、`arrowAirStreaks` 参数，支持外发光与破空线。
- `RenderSystem` 仅对弩塔 `sourceTowerType=9` 的 arrow 弹道注入新外观参数，不影响箭塔、剑雨等其他 arrow 表现。
- `public/sfx/tower_ballista.ogg` 替换为 OpenGameArt `80-CC0-RPG-SFX_0.zip` 中的 `blade_01.ogg`，许可证 CC0 / Public Domain；`public/sfx/CREDITS.md` 已补充来源映射。
- `Renderer.test.ts` 增加弩矢发光与破空线绘制回归测试。

### 验证

- `npm run typecheck` 通过。
- `npm test -- --run src/render/Renderer.test.ts src/systems/ProjectileSystem.test.ts src/systems/AttackSystem.test.ts` 通过，3 个测试文件共 79 条测试。

---

## 功能：弩矢特效接入 AI 生成贴图

时间：2026-06-17 10:28:40 CST

### 需求

弩矢特效使用 AI 生成图片素材替换程序绘制，同时保留原程序绘制效果。

### 改动

- 使用 `animal-mediakit` 生成弩塔弩矢特效图，生成模型为 `gemini/imagen-4.0-generate-001`；因当前团队无 `openai/gpt-image-1.5` 权限，先生成纯色背景素材再本地去背。
- 新增透明素材 `public/art/fx/fx_ballista_bolt.png`，并清理掉模型误生成的“60”文字，只保留弩矢主体、蓝色能量和速度线。
- `Renderer` 的 `arrow` 形状支持 `image` 主体：先绘制程序化外发光与破空线，再绘制旋转后的贴图；无贴图时继续走原几何箭矢。
- `RenderSystem` 中弩塔 projectile 优先加载 `fx_ballista_bolt.png` 作为 60px 主体，继续叠加程序化蓝光/破空线；贴图未加载时自动回退到程序化弩矢。
- `design/05-presentation.md` 更新弩塔弹道规范，明确贴图优先、程序化效果保留和回退策略。
- `Renderer.test.ts` 增加贴图主体 + 程序化特效共存的回归测试。

### 验证

- `npm run typecheck` 通过。
- `npm test -- --run src/render/Renderer.test.ts src/systems/ProjectileSystem.test.ts src/systems/AttackSystem.test.ts src/utils/imageCache.test.ts` 通过，4 个测试文件共 85 条测试。
- `public/art/fx/fx_ballista_bolt.png` 检查为 RGBA 透明 PNG，尺寸 895×192，四角 alpha 为 0，主体 alpha bbox 为 `(24, 24, 871, 168)`。

---

## 修复：补齐地震技能音效资源

时间：2026-06-17 10:16:45 CST

### 需求

检查代码和配置中出现的所有音效名字，确认是否存在对应音效资源；缺失资源需要使用开源音效补齐。

### 检查结果

- `src/utils/Sound.ts` 声明 96 个 SFX key，发现 `skill_earthquake` 缺少 `public/sfx/skill_earthquake.ogg`。
- `src/utils/Music.ts` 声明 19 个 BGM key，所有路径均存在；`battle_snow`、`battle_lava` 复用已有主题曲资源。
- `src/config` YAML 中的 `sound`、`audio.bgm`、`audio.sfx` 均可解析到已声明音效或已存在文件。

### 改动

- 新增 `public/sfx/skill_earthquake.ogg`，来源 OpenGameArt `sfx_100_v2.zip` 的 `sfx100v2_thunder_01.ogg`，许可证 CC0 / Public Domain。
- `Sound.ts` 移除 `skill_earthquake` 的 synth-only 标记，运行时改为使用实体 OGG 文件。
- `public/sfx/CREDITS.md` 补充地震技能音效来源与原始文件映射。
- `design/07-audio.md` 更新 SFX 覆盖数、版权说明、文件统计与版本信息。
- 新增 `src/utils/audioAssets.test.ts`，自动校验 SFX/BGM 路径实体文件，以及配置中的音效名称和音频文件路径。

### 验证

- `file public/sfx/skill_earthquake.ogg` 确认为 Ogg/Vorbis 音频。
- 资源路径脚本确认：SFX missing=0，BGM missing=0。
- `npm test -- src/utils/audioAssets.test.ts` 通过。
- `npm run typecheck` 通过。
