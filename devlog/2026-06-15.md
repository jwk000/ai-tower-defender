# 2026-06-15

## 修复：Boss 波只保留一个最终 Boss

### 需求

每一关最终 Boss 只能有一个，不能因为随机精英机制额外生成“精英巨型史莱姆”这类 Boss 变体。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/04-levels.md` | 明确 Boss 波只能配置 1 个最终 Boss，随机精英候选必须排除 Boss |
| `src/systems/WaveSystem.ts` | 重置精英候选时过滤 `isBoss` 敌人，Boss 波仍可从随从中生成精英，但不会复制 Boss 本体 |
| `src/systems/__tests__/WaveSystem.spawn.test.ts` | 增加 Boss 波回归测试，确认只生成 1 个巨型史莱姆，随机精英为随从哥布林 |
| `src/config/levels.test.ts` | 增加配置校验：每个 Boss 波恰好只有 1 个 Boss 敌人 |

### 验证结果

- `npm test -- src/systems/__tests__/WaveSystem.spawn.test.ts src/config/levels.test.ts` 通过。

## 技术咨询：游戏是否使用图集系统

### 结论

当前项目已经在运行时代码层支持图集 manifest 和图集帧绘制，但仓库 `public/art` 下尚未提交实际图集产物，因此实际运行时默认仍会回退到独立图片资源。

### 判断依据

- `src/utils/imageCache.ts` 会尝试读取 `/art/atlases/index.json`，并将旧资源路径映射到图集图片和源矩形。
- `getLoadedImageFrame()` 命中 manifest 时返回 `atlasId`、`source` 和图集图片；未命中或图集加载失败时回退独立图片。
- `drawImageFrame()` / `drawLoadedImage()` 已支持使用源矩形裁剪绘制图集帧。
- `src/utils/imageCache.test.ts` 已覆盖图集命中、单图回退、源矩形绘制和美术资源关闭场景。
- `design/10-art-atlas-pipeline.md` 定义了图集分包、manifest 格式、加载策略和验收标准。
- 当前 `public/art` 没有 `atlases` 目录，也没有实际 `index.json`、图集 PNG 或图集 JSON 产物。

### 结论表述

准确说法是：项目“有图集系统的运行时接入层和设计规范”，但“当前资源包还没有真正启用图集产物”。要让游戏实际使用图集，需要按 `design/10-art-atlas-pipeline.md` 生成并提交 `/public/art/atlases/index.json` 及对应图集图片。

---

## 修复：单位卡拖拽 ghost 使用场景单位外观

### 需求变化

卡牌从手牌拖动时，ghost 不应显示整张卡牌或卡牌主图。单位卡拖拽 ghost 应直接展示该单位在场景中的外观，便于玩家判断落点后的真实视觉效果。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/05-presentation.md` | 更新拖卡 UI 反馈规则，明确单位卡 ghost 必须使用场景单位外观，不显示卡牌框或卡牌主图 |
| `src/systems/UISystem.ts` | 移除拖拽时按手牌卡牌绘制整卡 ghost 的分支，复用原有单位主体、复合部件、武器、眼睛绘制路径 |
| `src/systems/UISystem.test.ts` | 将回归测试改为断言单位卡 ghost 不再绘制卡牌框/卡牌图，并绘制箭塔场景主体 |

### 验证结果

- `npm test -- src/systems/UISystem.test.ts` 通过，12 个测试。
- `npm run typecheck` 通过。
- `npm run build` 通过；保留 Vite 既有大 chunk 体积警告。

---

## 修复：拖拽 ghost 在美术资源开启时优先使用单位图片

### 需求变化

上一版已将拖拽 ghost 从卡牌外观改为场景单位外观，但只走了程序化绘制路径。实际期望是：当美术资源开关开启且单位图片已加载时，ghost 应与场景单位一样优先显示单位图片；只有美术资源关闭或图片不可用时才回退到程序化外观。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/05-presentation.md` | 补充拖卡 ghost 的图片优先规则，明确图片不可用或美术资源关闭时才回退程序化外观 |
| `src/systems/UISystem.ts` | 为拖拽 ghost 解析场景单位图片 ID，使用 `unitArtPath` 和 `getLoadedImageFrame` 优先绘制单位 idle 图片；失败时保留程序化复合外观回退 |
| `src/systems/UISystem.test.ts` | 增加美术资源开启时绘制单位图片、关闭时回退程序化外观的回归覆盖 |

### 验证结果

- `npm test -- src/systems/UISystem.test.ts` 通过，13 个测试。
- `npm run typecheck` 通过。
- `npm run build` 通过；保留 Vite 既有大 chunk 体积警告。

---

## 功能：生成图集并接入运行时图集加载

### 需求

为当前 `public/art` 资源生成实际图集产物，并让运行时代码显式引用图集加载路径，减少单位、地格、FX、UI 和图标资源的独立图片请求。

### 改动文件

| 文件 | 改动 |
|------|------|
| `scripts/build-art-atlases.mjs` | 新增图集构建脚本，基于 Pillow 生成 PNG 图集和 manifest |
| `package.json` | 新增 `npm run build:atlases` 命令 |
| `public/art/atlases/` | 新增 19 张图集及 JSON manifest，覆盖 503 个资源帧 |
| `src/utils/imageCache.ts` | 新增显式图集索引加载、图集图片预热和测试状态查询接口 |
| `src/main.ts` | 游戏启动时预加载图集索引，进关时预热图集图片 |
| `src/utils/imageCache.test.ts` | 增加预加载 atlas index 后通过图集帧解析资源路径的回归测试 |

### 生成结果

- 单位状态帧：拆成 `units_01` 到 `units_08`，覆盖 376 张 256×256 单位图。
- 主题地格：按 `meadow/desert/castle/wasteland/abyss` 各生成一张主题图集，运行时帧尺寸降为 256×256。
- 战斗通用：`fx_objectives` 覆盖 FX 和水晶/传送门目标点资源。
- UI/Icon：`icons_common` 覆盖 Buff 与 card-icon；`ui_common_01` 到 `ui_common_04` 覆盖 UI 基础件。
- `public/art/atlases/index.json` 作为运行时统一入口，manifest key 保留旧资源路径，业务代码无需改配置路径。

### 验证结果

- `npm run build:atlases` 通过，生成 19 张图集、503 个帧映射。
- `npm test -- src/utils/imageCache.test.ts src/utils/artAssets.test.ts` 通过，2 个测试文件、11 个用例。
- `npm test -- src/utils/imageCache.test.ts src/systems/RenderSystem.crystal.test.ts src/systems/UISystem.test.ts` 通过，3 个测试文件、23 个用例。
- `npm run typecheck` 通过。
- `npm run build` 通过；保留 Vite 既有大 chunk 体积警告。

---

## 重设计：按 MDA 理论重平衡全关卡数值

### 需求

按 MDA 理论重新设计 5 个关卡的数值，覆盖卡池、波次奖励、敌人数量与高威胁单位并发压力，保证在最坏金币掉落情况下玩家仍有资源构建基础防线，不出现资源死局。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/04-levels.md` | 新增 MDA 数值重设计计划，记录最坏预算口径、调整原则和每关策略 |
| `src/config/levels/*.yaml` | 调整 5 关初始金币、波次奖励、高威胁敌人数量与第4关卡池 |
| `src/editor/state/modelToLevelConfig.ts` | 透传 YAML 波次 `reward` 到运行时 `WaveConfig.reward` |
| `src/config/levels.test.ts` | 增加敌人引用、波次奖励透传、最坏金币下限预算校验 |

### 关键决策

- 敌方黑暗牧师统一使用 `dark_priest`，避免关卡波次误引用玩家祭司 `priest`。
- 预算按 `初始金币 + 击杀金币下限(80%) + 波次奖励` 计算，并把每波额外随机精英纳入最坏金币下限。
- 降低第3-5关后段牧师/坦克/无人机/巨型机器人等高威胁单位的多路叠压，保留主题压力但给玩家可恢复窗口。

### 验证结果

- `npm test -- src/config/levels.test.ts` 通过，8 个测试。
- `npm run typecheck` 通过。

---

## 设计：补全低空单位与防空对策说明

### 需求

确认每一关哪些敌人属于低空单位，以及哪些塔/单位能攻击低空单位。现有文档中 LowAir 信息分散在单位表和关卡波次里，缺少统一白名单和关卡验收表。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/03-units.md` | 新增低空攻击规则，明确 LowAir 对空白名单与不可对空单位，并为相关塔/士兵补充“可攻击低空”标注 |
| `design/04-levels.md` | 新增低空单位与防空对策总览，按关卡列出 LowAir 敌人、出现波次、初始卡池对策和设计结论 |

### 关键结论

- LowAir 敌人为：吸血蝗虫、吸血蝙蝠、无人机、飞机。
- 可稳定攻击 LowAir 的单位为：箭塔、弩塔、激光塔、蝙蝠塔、电塔、弓手。
- 导弹塔定位为地面战略爆炸，不处理 LowAir；炮塔、冰塔、火塔、毒塔、法师和地面机关也不命中 LowAir。
- 第2关从第3波引入低空教学，第3-5关均需要卡池或抽牌保底提供防空能力。

### 验证结果

- 已通过文档和配置检索确认 `locust`、`vampire_bat`、`plane`、`drone` 在设计/配置中对应 LowAir 敌人。

---

## 数值：提高塔投资成本并拉开士兵职责差异

### 需求

提高塔的价格和 HP，降低士兵价格，并显著拉开士兵数值与技能差异：盾卫高血高防低攻，刺客低血高攻并处决低血普通敌人，弓手超远射程和暴击，法师施加负面 Buff 并周期施法，祭司改名牧师并治疗士兵，工程师修塔且无攻击力。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/03-units.md` | 新增本轮经济与差异化说明，更新 6 个士兵定位、数值和卡牌表 |
| `src/config/units/towers.yaml` / `src/config/cards/towers.yaml` | 同步上调塔 HP 与建造价格 |
| `src/config/units/soldiers.yaml` / `src/config/cards/soldiers.yaml` | 下调士兵价格，重做盾卫、弓手、牧师、工程师、刺客、法师数值与特殊字段 |
| `src/data/cards.ts` / `src/data/gameData.ts` | 同步运行时静态卡牌与单位配置，补入工程师手牌 |
| `src/data/levels/soldierBridge.ts` / `src/types/index.ts` | 支持 YAML 注入暴击、处决、Debuff、周期法术、治疗和修塔字段 |
| `src/systems/SoldierAISystem.ts` | 实现士兵暴击、刺客处决、牧师治疗、工程师修塔、法师周期法术与近战 Debuff |
| `src/core/components.ts` / `src/systems/ProjectileSystem.ts` | 为士兵远程投射物增加 Debuff 元数据，确保法师远程命中也能施加负面 Buff |
| `src/systems/SoldierAISystem.test.ts` | 增加差异化士兵机制回归测试 |

### 关键决策

- 塔改为高价高血量的长期阵地资产；士兵改为低价战术补位，降低最坏卡序下的防线断档风险。
- 刺客的处决只对普通敌人生效；精英和 Boss 仍会受到普通攻击，但不会被 10% 阈值直接处决。
- 法师 Debuff 既覆盖周期法术，也覆盖远程投射物命中，避免“配置有描述、运行时无效果”。
- 工程师不参与攻击，治疗/修复目标通过 `Category` 区分，牧师只治疗士兵，工程师只修复塔。

### 验证结果

- `npm test -- src/systems/SoldierAISystem.test.ts src/config/cards.test.ts src/systems/ProjectileSystem.test.ts` 通过，3 个测试文件、154 个用例。
- `npm run typecheck` 通过。
- `npm run build` 通过；保留 Vite 既有大 chunk 体积提示。

---

## 功能：实现低空单位攻击规则

### 需求

按设计实现 LowAir 低空单位规则：只有箭塔、弩塔、激光塔、蝙蝠塔、电塔、弓手能主动选择并命中低空单位；炮塔、导弹塔、冰塔、火塔、毒塔、法师和地面机关不能命中 LowAir。初始手牌需要保证至少有一个低空对策。

### 改动文件

| 文件 | 改动 |
|------|------|
| `src/core/components.ts` | `Attack` 组件新增 `canTargetLowAir` 字段，显式表达对空能力 |
| `src/utils/lowAirTargeting.ts` | 新增低空对策白名单工具，统一塔、士兵和卡牌判断 |
| `src/data/gameData.ts` / `src/data/levels/towerBridge.ts` | 为塔/士兵配置写入 `canTargetLowAir`，YAML 注入路径同步计算白名单 |
| `src/systems/UnitFactory.ts` | 创建塔、士兵、陷阱时写入运行时对空能力 |
| `src/systems/AttackSystem.ts` | 目标选择和层级可达性改为“远程 + canTargetLowAir”才能攻击 LowAir |
| `src/systems/ProjectileSystem.ts` | 地面爆炸/溅射/连锁二次命中不能绕过 LowAir 对空限制 |
| `src/systems/HandSystem.ts` | 初始手牌保底至少 1 张 LowAir 对策牌 |
| `src/systems/*.test.ts` | 更新并新增低空攻击、溅射免疫、初始手牌保底测试 |

### 验证结果

- `npm run typecheck` 通过。
- `npm test -- src/systems/AttackSystem.test.ts src/systems/ProjectileSystem.test.ts src/systems/MissileTargeting.test.ts src/systems/TrapSystem.test.ts src/systems/BatSwarmSystem.test.ts src/systems/BossSystem.test.ts src/systems/EnemySkillSystem.test.ts src/core/RuleHandlers.test.ts src/systems/__tests__/HandSystem.test.ts src/config/levels.test.ts` 通过，10 个测试文件、203 个用例。
- `npm run build` 通过；保留 Vite 既有大 chunk 体积提示。
- `git diff --check` 通过。

---

## 美术：重做塔攻击帧并按塔拆分图集

### 需求

塔是建筑，攻击动作图片不应该发生主体位移；攻击帧只体现炮口火光、弓弦蓄光、晶体亮度、线圈电光、毒囊气泡、发射管热光等塔体自身细节变化。子弹、箭矢、导弹、激光束等投射物由运行时投射物系统绘制，不烘焙进塔攻击帧。同时重新生成塔攻击帧和图集，图集打包改为每个塔一个图集并缩小单图集尺寸。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/09-ai-art-redesign.md` | 明确塔/建筑攻击帧主体锚点、底座和整体重心不位移，仅变化发射细节 |
| `design/10-art-atlas-pipeline.md` | 更新图集规范：玩家塔每塔一张图集，塔图集宽度上限 1024 |
| `scripts/regenerate_tower_attack_frames_ai.py` | 新增 AI 塔攻击帧生成器，以 idle 帧为参考重新生成 attack sheet，再切片去绿底 |
| `scripts/build-art-atlases.mjs` | 将 `unit_tower_<type>_*` 从通用 units 图集中剥离，生成 `public/art/atlases/towers/tower_<type>.png/json` |
| `public/art/units/unit_tower_*_attack_*.png` | 重新生成 10 种塔的攻击帧 |
| `public/art/atlases/**` | 重新生成图集和 index manifest |

### 验证结果

- `python3 scripts/regenerate_tower_attack_frames_ai.py --force` 通过，AI 重新生成 10 种塔的 20 张攻击帧。
- `node scripts/build-art-atlases.mjs` 通过，生成 28 张图集；10 张塔图集均为 776×776。
- 自检确认通用 `units_*` 图集内不再包含 `unit_tower_` 帧，塔帧全部进入对应 `tower_<type>` 图集。
- 目视检查 `tmp/tower_attack_review_no_projectiles.png`：塔攻击帧不再绘制离体投射物，激光塔两张 attack 帧尺寸一致，仅保留核心和镜头亮度变化。
- `npm run typecheck` 通过。
- `npm test -- --run src/utils/imageCache.test.ts src/utils/artAssets.test.ts` 通过，2 个测试文件、11 个用例。

---

## 删除：金矿和能量塔单位

### 需求

从当前可玩内容中删除金矿和能量塔两个生产建筑单位，并同步移除对应生产卡牌入口，避免卡池引用不存在的单位。

### 改动文件

| 文件 | 改动 |
|------|------|
| `src/config/units/buildings.yaml` | 删除 `gold_mine`、`energy_tower` 单位配置 |
| `src/config/cards/production.yaml` | 删除 `gold_mine_card`、`energy_tower_card` 生产卡配置 |
| `src/config/cards.test.ts` | 更新 B3 卡牌测试，仅保留地刺陷阱卡期望 |
| `src/utils/SaveManager.ts` | 初始解锁卡池移除 `gold_mine_card` |
| `src/types/index.ts` | 移除 `ProductionType` 中的金矿/能量塔枚举项，改为预留字符串类型 |
| `src/systems/RenderSystem.ts` | 删除生产实体到金矿/能量塔资源 ID 的兜底映射 |
| `src/config/cardRegistry.ts` | 更新 `production` 卡牌类型注释，标记为当前未配置的预留类型 |
| `design/05-presentation.md` | 选中交互说明移除生产建筑描述 |

### 验证结果

- `npm test -- src/config/cards.test.ts` 通过，90 个用例。
- `npm run typecheck` 通过。

---

## 美术：删除塔移动帧并重建图集

### 需求

塔是建筑，不会移动，因此不需要 `move_0` / `move_1` 动作帧。删除这些无用的塔移动帧，并重新生成图集。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/09-ai-art-redesign.md` | 明确塔只交付 `idle`、`attack`、`death` 三个状态共 6 帧，不生成 `move` |
| `design/10-art-atlas-pipeline.md` | 更新塔图集规范：每塔图集只包含 `idle`、`attack`、`death`，典型尺寸约 776×518 |
| `scripts/build-art-atlases.mjs` | 塔图集识别后缀移除 `move_0` / `move_1` |
| `public/art/units/unit_tower_*_move_*.png` | 删除 10 种塔的 20 张移动帧 |
| `public/art/atlases/**` | 重新生成图集和 index manifest |

### 验证结果

- `node scripts/build-art-atlases.mjs` 通过，生成 27 张图集、473 帧。
- 自检确认 10 个塔图集均为 6 帧、776×518，且 `index.json` 中没有 `unit_tower_*_move_*` 引用。
- `npm run typecheck` 通过。
- `npm test -- --run src/utils/imageCache.test.ts src/utils/artAssets.test.ts` 通过，2 个测试文件、11 个用例。

---

## 美术：整理机关动作状态机并重做机关动作帧

### 需求

机关都不会移动，不需要生成机关移动动作；巨石只能被攻击，不需要生成攻击动作。整理机关动作状态机和美术需求，并重新生成机关图片资源。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/09-ai-art-redesign.md` | 明确普通机关只交付 `idle/attack/death`，巨石只交付 `idle/death`；`attack` 对普通机关表示触发动作 |
| `scripts/generate_unit_action_frames.py` | 增加机关状态 profile，生成脚本不再为机关生成无意义的 `move` 帧，也不再为巨石生成 `attack` 帧 |
| `src/systems/RenderSystem.ts` | 机关渲染按 `Trap.animTimer` 播放触发用 `attack` 帧；巨石始终使用 `idle`，死亡继续走死亡帧 |
| `public/art/units/unit_*trap*_*.png` / `public/art/units/unit_boulder_*.png` | 重做地刺、捕兽夹、焦油坑触发/死亡帧和巨石死亡帧，删除机关移动帧与巨石攻击帧 |
| `public/art/atlases/units/*` / `public/art/atlases/index.json` | 重建单位图集，移除已删除机关帧引用 |

### 验证结果

- `python3 scripts/generate_unit_action_frames.py --only spike_trap bear_trap tar_pit boulder --preserve-idle --force` 通过，按机关状态 profile 生成图片。
- `node scripts/build-art-atlases.mjs` 通过，生成 27 张图集、493 个帧引用。
- `rg "unit_(spike_trap|bear_trap|tar_pit)_move_|unit_boulder_(move|attack)_" public/art/atlases public/art/units src design scripts` 无结果。
- `npm run typecheck` 通过。
- `npm test -- src/utils/artAssets.test.ts src/utils/imageCache.test.ts src/systems/RenderSystem.crystal.test.ts src/utils/VisualAudit.test.ts` 通过，4 个测试文件、34 个用例。
- `npm run build` 通过；保留 Vite 既有大 chunk 体积提示。

---

## 修复：空地继续显示移动伤害飘字

### 问题现象

地面上已经看不到敌方单位时，仍然能看到黄色伤害数字继续向上移动，容易误判为空地或隐藏单位仍在受伤。

### 根因

伤害数字由 `DamageNumberSystem` 作为独立 ECS 特效实体创建，并在 `onPostRender` 阶段覆盖绘制。伤害事件先进入延迟队列，后续系统消费队列时如果目标已经死亡或被清理，旧实现仍会读取目标位置并生成新的伤害数字。

### 改动文件

| 文件 | 改动 |
|------|------|
| `src/systems/DamageNumberSystem.ts` | 生成目标伤害数字前检查目标实体仍存在、仍有 `Position` / `Health`，且 `Health.current > 0` |
| `src/systems/DamageNumberSystem.test.ts` | 新增目标存活、目标已死亡、目标已清理三类回归测试 |

### 验证结果

- `npm test -- DamageNumberSystem` 通过，3 个测试。
- `npm run typecheck` 通过。

---

## 美术：重做机关攻击两态动作帧

### 需求

地刺攻击动作需要由“地刺收起状态”和“地刺发出状态”组成；其他机关单位的攻击动作也按同样思路设计，确保 `attack_0` 是收起/待触发，`attack_1` 是发出/触发峰值。

### 改动文件

| 文件 | 改动 |
|------|------|
| `scripts/generate_unit_action_frames.py` | 为地刺、捕兽夹、焦油坑补充机关专用攻击帧 prompt，明确 `attack_0` / `attack_1` 的触发前后语义 |
| `public/art/units/unit_spike_trap_attack_*.png` | 重做地刺攻击帧，`attack_0` 为低伏收起地板态，`attack_1` 为尖刺刺出态 |
| `public/art/units/unit_tar_pit_attack_*.png` | 重做焦油坑攻击帧，`attack_0` 为低伏焦油面，`attack_1` 为黏液柱/触须喷出态 |
| `public/art/atlases/units/units_06.png` | 重建包含机关帧的单位图集 |

### 验证结果

- `python3 scripts/generate_unit_action_frames.py --only spike_trap tar_pit --preserve-idle --force` 通过。
- 目视检查 `tmp/trap-action-preview.png`：地刺和焦油坑 `attack_0` / `attack_1` 已形成明显“收起/发出”对照，捕兽夹维持“张开/合拢”对照。
- `node scripts/build-art-atlases.mjs` 通过，生成 27 张图集、473 帧。
- `rg "unit_(spike_trap|bear_trap|tar_pit)_move_|unit_boulder_(move|attack)_" public/art/atlases public/art/units src design scripts` 无结果。
- `npm run typecheck` 通过。
- `npm test -- src/utils/artAssets.test.ts src/utils/imageCache.test.ts src/utils/VisualAudit.test.ts` 通过，3 个测试文件、29 个用例。

---

## 美术：场景装饰图片化与路径地格去重

### 需求

场景装饰物从程序绘制升级为美术图片，并按动作复杂度提供 1-4 帧 `idle` 序列帧动画；装饰物图片与同主题场景地格合入同一主题图集。路径地格只保留每主题一个方向无关图片，删除重复的直线、转角、T 字和十字路径变体。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/05-presentation.md` / `design/09-ai-art-redesign.md` / `design/10-art-atlas-pipeline.md` | 明确装饰图片命名、帧数规则、程序化兜底和主题图集合包规范 |
| `src/utils/decorArt.ts` | 新增装饰物帧数、路径生成和旧别名 `crystal` / `rift` 兼容 |
| `src/systems/DecorationSystem.ts` | 美术资源开启且图片帧可用时绘制装饰序列帧；关闭开关或图片缺失时继续走原程序几何绘制 |
| `scripts/generate-ai-art-assets.mjs` | 新增 `--type` 过滤参数和当前关卡装饰物生成清单 |
| `scripts/build-art-atlases.mjs` | 主题图集合并 `tiles + decor`，并排除废弃路径方向变体 |
| `public/art/decor/*.png` | 生成 50 张当前关卡装饰物图片帧 |
| `public/art/tiles/*_path_{straight,corner,tee,cross}*.png` | 删除重复路径方向变体 |
| `public/art/atlases/themes/*` / `public/art/atlases/index.json` | 重建主题图集，包含地格与装饰帧 |

### 验证结果

- `node scripts/generate-ai-art-assets.mjs --type=decor --priority=P0 --limit=80` 通过，50 张装饰图全部完成。
- `npm run build:atlases` 通过，生成 27 张图集、468 帧。
- 脚本检查 5 个主题图集均包含 `decor_` 帧，且 `pathVariants: 0`。
- `find public/art/tiles ...path direction variants... | wc -l` 输出 0。
- `npm test -- src/utils/decorArt.test.ts src/utils/pathTileTexture.test.ts src/utils/imageCache.test.ts` 通过，3 个测试文件、15 个用例。
- `npm run typecheck` 通过。
- `npm run build` 通过；保留 Vite 既有大 chunk 体积提示。

---

## 修正：装饰静态变体、粒子动效与资源流水线

### 需求

装饰物不再把已生成的 `idle_1/2/3` 当作时间轴动画帧播放，而是作为静态外观变体按位置确定性选取；动态效果改由运行时粒子实现。已有生成图片需要去掉白色/棋盘/地格背景，并把“生成图片 -> 抠背景 -> 打包图集”固化为工具链。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/05-presentation.md` / `design/09-ai-art-redesign.md` / `design/10-art-atlas-pipeline.md` | 补充静态变体、粒子动效、白底抠图和资源流水线规范 |
| `src/utils/decorArt.ts` / `src/utils/decorArt.test.ts` | 保留 `_idle_N` 资源路径，新增静态变体数量、确定性变体选择和粒子类型分类 |
| `src/systems/DecorationSystem.ts` | 美术资源开启时按 `row/col/type/theme` 选一张静态变体，图片缺失回退 `idle_0`，再缺失回退程序化绘制；图片绘制成功后叠加低密度粒子 |
| `scripts/generate-ai-art-assets.mjs` | 装饰资源继续输出 `_idle_N` 多张静态变体，并在 prompt 中明确不是动画帧 |
| `scripts/process-ai-art-pipeline.mjs` / `package.json` | 新增 `npm run art:pipeline`，串联生成、抠背景和图集打包 |
| `public/art/decor/*.png` / `public/art/atlases/themes/*` | 处理 50 张装饰图的白色/棋盘/地格背景并重建主题图集 |

### 验证结果

- `npm run art:pipeline -- --skip-generate --target=public/art/decor --threshold=52 --soft=34` 通过，50 张装饰图全部执行边缘连通背景透明化，重建 27 个图集、468 帧。
- 图集校验通过：主题图集包含 50 个装饰 frame，路径方向变体数量为 0。
- 透明度校验通过：50 张装饰图边缘不透明像素均为 0，保留物件不透明区域约 11%-32%。
- `npm test -- src/utils/decorArt.test.ts src/utils/pathTileTexture.test.ts src/utils/imageCache.test.ts` 通过，3 个测试文件、16 个用例。
- `npm run typecheck` 通过。

---

## 修正：部署地格占用规则

### 需求

士兵放置在路径格时永远无视占用；机关不能叠放在同一个地格，但机关死亡后释放占用；装饰物占用地格，塔不能放在装饰物上；塔死亡后释放塔位占用。

### 改动文件

| 文件 | 改动 |
|------|------|
| `design/02-gameplay.md` / `design/03-units.md` | 明确士兵、机关、塔和装饰物的占用规则 |
| `src/systems/BuildSystem.ts` | 按实体类型拆分占用判定：士兵忽略占用，机关按 Trap 组件互斥，塔检查 Ground 占用和装饰物 |
| `src/main.ts` | 移除士兵部署时对 `GridOccupant` 的拒绝逻辑 |
| `src/systems/__tests__/BuildSystem.cost-decoupling.test.ts` | 增加机关死亡释放、塔死亡释放、装饰物阻挡塔位的回归测试 |

### 验证结果

- `npm run typecheck` 通过。
- `npx vitest run src/systems/__tests__/BuildSystem.cost-decoupling.test.ts` 通过，7 个测试。
